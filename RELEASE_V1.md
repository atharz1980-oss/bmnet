# Bayt Almosawer — V1 Production

> نسخة مثبَّتة. ما دونه مقيس من الكود وقاعدة الإنتاج والموقع الحي، لا منقول
> من تقرير سابق.

| البند | القيمة |
|---|---|
| Release date | 2026-09-14 |
| Release tag | `v1.0.0` |
| آخر إيداع كود | `9f7a1d8c84c016761c3082012628561650a00d73` |
| الإيداع الموسوم | هذا الإيداع — `v1.0.0` يشير إليه |
| Production domain | https://baytalmosawer.net |
| آخر ترحيل | `supabase/migrations/20260914090000_atomic_course_save.sql` |
| آخر RPC | `public.save_course_atomic(uuid, jsonb, jsonb, jsonb)` |
| سجل الترحيلات | 15 متزامنًا · صفر معلّق |
| CRITICAL | **لا شيء** |
| HIGH | **لا شيء** |

---

## ما تتضمنه النسخة

**Public website** — 18 مسارًا عامًا يُفتح بلا حساب: الرئيسية، من نحن،
الدورات وتفاصيلها، المسارات وتفاصيلها، المدونة ومقالاتها، التواصل، تدريب
الشركات، أربع صفحات سياسات، المجتمع، المصورون، والملفات العامة. عربي RTL،
canonical على كل صفحة، sitemap بلا مسارات خاصة، robots يمنع `/admin`،
وصفر عناوين داخلية مسرَّبة.

**Admin/CMS** — لوحة تحكم بصلاحيات على مستوى الوحدة والفعل. التعديل من
اللوحة يظهر على الموقع الحي ويُسترجع حرفيًا: الإعدادات العامة، بيانات
التواصل، وسائل التواصل، المدونة، محتوى الرئيسية، والدورات.

**Authentication** — جلسة Supabase واحدة للموقع واللوحة. الموقع **عام
أولًا**: نطاق الـmiddleware `/admin` و`/community` و`/account` فقط، فلا
صفحة عامة تدفع رحلة مصادقة. الدخول يُطلب عند الفعل وحده ويعيد المستخدم
إلى حيث كان. المصادقة ليست صلاحية إدارة: دخول اللوحة يتحقق من ملف إدارة
نشط ويرفض غيره برسالة صريحة.

**Password recovery** — ثلاثة مسارات تحت `/community/reset-password`.
الاستقبال Route Handler يقبل `code` و`token_hash` ورموز الجزء بعد `#`.
لا تعداد حسابات (رسالة واحدة لكل الحالات)، ولا عميل خدمة في التدفق،
ووجهة الرابط من نطاق الموقع لا من ترويسة الطلب، وحد 3 طلبات/15 دقيقة.

**Community** — ملفات المصورين، الخلاصة، المنشورات، الإعجاب، التعليق،
الحفظ، المتابعة، الإشعارات، الاكتشاف، البلاغات، والحجب مع قطع المتابعة
ومنع المحجوب من التفاعل. صلاحيات مفصولة بين الزائر والعضو وصاحب المحتوى
والإشراف.

**Portfolio** — مشاريع أعمال بوسائط متعددة وغلاف، تُعرض في الملف العام،
وإلغاء النشر يقطع الوصول إلى وسائطها.

**Community media security** — `community-media` bucket خاص، ولا رابط
تخزين عام. كل عرض يمر بمسار `/community/media` يفحص الظهور **عند كل
طلب**، فالإخفاء والتعليق وإلغاء النشر والحذف تقطع خلال أقل من ثانية
ونصف. ولا تمر هذه الوسائط بمحسِّن الصور إطلاقًا — المحسِّن يحتفظ بنسخته
ويخدمها بـ`public, max-age=14400` فكان يُبطل الإشراف. `bm-media` لم يُمَس
ويبقى محسَّنًا.

**Atomic course save** — حفظ الدورة وأطفالها في معاملة واحدة عبر
`save_course_atomic`. كان حذفًا ثم إدراجًا عبر طلبات منفصلة، فأي فشل بعد
الحذف يمحو المنهج والمواعيد — وقع فعلًا. والأطفال تُطابَق بالمعرّف فلا
تُفنى الهوية، وبهذا لم يعد حفظ الدورة يفكّ تثبيت موعد الرئيسية.

**Security headers** — `X-Frame-Options: SAMEORIGIN` و
`X-Content-Type-Options: nosniff` و`Referrer-Policy:
strict-origin-when-cross-origin`، و`x-powered-by` معطّلة.

**Supabase modern keys** — `sb_publishable_*` في المتصفح و`sb_secret_*`
على الخادم وحده. لا مفتاح JWT قديم، والمفاتيح القديمة معطّلة. صفر سر خدمة
في حزم المتصفح المنشورة (22 حزمة مفحوصة).

**RLS** — مفعّلة على كل جداول المجتمع والإدارة. الزائر لا يقرأ 11 جدولًا
خاصًا ولا يكتب، والعضو لا يمس بيانات غيره ولا يرفع في مجلد غيره ولا يرفع
صلاحيته. منحة `UPDATE` على `community_content_reports` أوسع من اللازم لكن
RLS يرشّحها فلا تمنح قدرة فعلية — مؤكد عمليًا.

**Resend SMTP** — مضبوط في Supabase ومُثبت يدويًا من المالك ببريد حقيقي.
لم يُمَس في هذا الإصدار.

---

## Known Issues

هذه معروفة ومقبولة في V1 — لا CRITICAL ولا HIGH بينها.

1. **حفظ الصفحة الرئيسية غير ذرّي** [MEDIUM] — يكتب 14 جدولًا خارج معاملة
   واحدة. فشل جزئي يترك حالة مختلطة. نفس علة حفظ الدورة التي أُغلقت،
   وعلاجها RPC مماثلة.
2. **تأخيرات CDN** [MEDIUM/LOW] — CDN هوستنجر يخدم الصفحات الساكنة بـ
   `s-maxage=300`، فتعديل CMS قد يتأخر حتى 5 دقائق على الرئيسية وقوائم
   الدورات والمدونة. وحذف كائن من `bm-media` يبقى مخدومًا حتى ساعة. كما
   يُخزَّن رفض وسائط المجتمع (404) نحو دقيقة، فاسترجاع محتوى مخفي يتأخر
   بمقدارها — والتطبيق نفسه يعيد 200 فورًا (مؤكد بكاسر تخزين).
3. **استعادة كلمة المرور بنفس المتصفح** [LOW] — `@supabase/ssr` يستخدم
   PKCE فيخزّن مُتحقِّق الرمز في كوكي المتصفح الذي طلب الاستعادة. فتح
   الرابط من جهاز آخر يفشل ويُعرض «اطلب رابطًا جديدًا». مسار الاستقبال
   يقبل `token_hash` أصلًا، فتبديل قالب البريد إلى `{{ .TokenHash }}`
   يحلّها بلا تغيير كود.
4. **CSP/HSTS غير مكتملين** [LOW] — الاستضافة ترسل
   `upgrade-insecure-requests` فقط. CSP كاملة تحتاج جرد مصادر قبل فرضها،
   وHSTS قرار نطاق يخص الاستضافة لا التطبيق.

---

## Rollback reference

النشر يتم بسحب هوستنجر التلقائي من `main`. للرجوع: أعِد `main` إلى الوسم
أو الإيداع المطلوب وادفع، ثم انتظر النشر.

| نقطة | الإيداع | ماذا تشمل |
|---|---|---|
| **V1 (الحالية)** | `9f7a1d8` | حفظ الدورة الذرّي |
| قبل الذرّية | `a9bce9d` | ترويسات الأمان — حفظ الدورة غير ذرّي |
| قبل الترويسات | `eee4651` | استعادة كلمة المرور مكتملة |
| قبل الاستعادة | `588a350` | إغلاق ثغرة محسِّن الصور |

**تنبيه على الرجوع:** ترحيل `20260914090000` يضيف دالة ولا يحذف شيئًا،
فالرجوع في الكود آمن ولا يستلزم تراجعًا في القاعدة — الكود الأقدم لا
يستدعي الدالة فحسب. أما ترحيل `20260912190000` (جعل `community-media`
خاصًا) فالرجوع عنه يعيد فتح وسائط المجتمع للعامة؛ لا ترجع عنه.

---

## Verification summary

| المجال | الدليل |
|---|---|
| الموقع العام | 69/69 على الإنتاج الحي |
| المصادقة و«عام أولًا» | 53/53 |
| استعادة كلمة المرور | 18/18 حيًا بلا إرسال بريد + 31 اختبارًا ساكنًا |
| المجتمع | 67/73 ثم 12/13 بعد تصحيح ترتيب الهيكل |
| أمان وسائط المجتمع | صفر وسائط عبر المحسِّن · الإخفاء يقطع <1.5s · 5 محاولات بلا تسرب |
| RLS والمفاتيح والأمان | 61/62 (الوحيد كان `x-powered-by` وأُصلح) + RLS الحي 38/38 |
| حفظ الدورة الذرّي | 24/24 (نجاح + تراجع قسري + تفويض + تزامن) و11/11 smoke على الإنتاج |
| البوابات | TypeScript 0 · ESLint 0 · 290 اختبارًا/982 assertion · بناء 71/71 |
| نظافة الإنتاج | صفر بيانات اختبار · كل جداول المجتمع صفر · `bm-media` 14 كائنًا سليمة |

**الحسابات على الإنتاج وقت التثبيت:** أربعة — حسابا الموظفين
(`owner@baytalmosawer.sa`، `accsbc@hotmail.com`) وحسابان سجّلهما المالك
يدويًا لاختبار التسجيل والاستعادة. لا حسابات اختبار آلية.

---

## ما يستحق العمل بعد الإطلاق

1. **غيّر كلمة مرور حساب الإدارة** إن لم تكن غُيّرت — وردت في سجل جلسة عمل.
2. **اجعل حفظ الصفحة الرئيسية معاملة ذرّية** — آخر بقايا العلة نفسها.
3. **راجع النسخ الاحتياطي وخطة الاستعادة** من لوحة Supabase قبل دخول
   أعضاء حقيقيين.
