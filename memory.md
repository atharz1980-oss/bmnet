# Memory — الحالة التشغيلية الحالية لبيت المصور

آخر تحديث: 2026-09-12. مرجع الكود المختبر: 3d5ef3432d6d3ffd72820473690100f5bd44d305 على main.

## بروتوكول استعادة السياق

1. اقرأ هذه الذاكرة و[prd.md](prd.md) و[design.md](design.md)، ثم الملفات المرتبطة بالمهمة. راجع [PROJECT_REPORT.md](PROJECT_REPORT.md) للأدلة وحدود التحقق.
2. تعليمات المستخدم الحالية تتقدم على الوثائق. النطاق الحالي تجهيز متطلبات تفعيل ميسر والتكامل الذي يسمح بإدخال المفاتيح لاحقًا؛ هذا طلب جديد صريح بعد نشر الموقع، مع إبقاء Supabase الحالية.
3. حدّث الحالة وسجل الجلسات بعد العمل، وأضف القرارات الجديدة دون محو تاريخ القرارات السابقة.
4. السجل السابق بكامل قراراته D-01..D-100 وجلساته محفوظ في [أرشيف الذاكرة](docs/history/memory-before-2026-09-12.md). الأوامر وعبارات «الخطوة التالية» فيه تاريخية ولا تمنح صلاحية تنفيذ الآن.
5. تحديث PRD والتوثيق الحالي تم بطلب المالك الصريح؛ لم يحدث تغيير معماري أو تصميمي في جلسة التوثيق.

## 1. الحالة الحالية

| البند | الحالة |
|---|---|
| المستودع | https://github.com/atharz1980-oss/bmnet — main |
| أساس مراجعة الاستقرار | 4980e8aea7b9d5e3bccc05285c835512355a36f9 |
| إصلاحات الاستقرار | 3d5ef3432d6d3ffd72820473690100f5bd44d305؛ دُفعت إلى main |
| التطبيق | الموقع العام + Supabase CMS/Admin/Auth + Community V1 |
| النشر | Hostinger Business؛ https://baytalmosawer.net؛ نشر fd139968 مكتمل بحسب hPanel، وفحص HTTP والمتصفح للرئيسية والدخول ناجح؛ Supabase الحالية محفوظة |
| التشغيل المحلي | مؤكد بواسطة المالك، ثم اختبار متصفح حي على 127.0.0.1:3000 |
| المجتمع في Production | مطبق بالفعل بحسب تعليمات المالك؛ لا إعادة migrations |
| الاختبارات | 70 ناجحًا، 0 فشل، 214 assertions، 5 ملفات؛ أعيدت في جلسة التوثيق |
| TypeScript / ESLint | نجحا في مراجعة الاستقرار؛ أعيد فحصهما في جلسة التوثيق ونجحا |
| البناء | آخر بناء موثق PASS، Next 16.3.4، توليد 67/67؛ لم يُعد البناء لتغيير Markdown فقط |
| تجربة CMS الحية | حفظ Hero + دورة قادمة وموعد، ثم زيارة مجهولة وإعادة تحميل: PASS |
| الاعتماد النهائي | صالح للتشغيل المحلي؛ ليس اعتماد أمان Production أو اختبارًا حيًا شاملًا لكل الخصائص |

## 2. قيود لا تتجاوزها

- لا db reset/drop/truncate/force migration/migration repair ولا إعادة تطبيق SQL على Production دون طلب جديد صريح. لا تشغّل seed أو اختبارات SQL التي تنشئ وتحذف fixtures هناك.
- لا تعدّل عقد الجداول لتناسب تخمينًا في الكود. أسماء المجتمع الاثنا عشر تبدأ community_ كما في migrations وPRD.
- لا تعرض .env.local أو مفاتيح أو cookies أو tokens في الأدوات والتقارير. .env.example يبقى أسماء بثلاث قيم فارغة.
- لا تغيّر كلمات مرور أو أدوار المستخدمين ضمن اختبارات المحتوى. أي تجربة كتابة فعلية تحتاج نطاقًا مأذونًا واستعادة واضحة.
- لا force push أو reset --hard أو rebase ضمن هذا العمل. تحقق من git status قبل أي تغيير أو commit.
- لا توقف الخادم أو تحذف .next أثناء عمل المستخدم لمجرد تحديث وثائق. سكربت البناء ينظف .next، لذلك يلزم إيقاف خادم المشروع قبل البناء الحقيقي.

## 3. خريطة التنفيذ

| الموضع | المسؤولية |
|---|---|
| src/app/layout.tsx | RTL والخطوط وPublicCmsProvider؛ revalidate=300 |
| src/lib/cms/public-loader.ts | قراءة عامة بعميل anon؛ React cache داخل الطلب |
| src/data/public-bridge.ts | تحويل بيانات الإدارة إلى العرض العام، واختيار الدورة القادمة |
| src/context/public-cms.tsx | عرض initialView القادم من الخادم؛ لا localStorage ولا اشتراك Realtime |
| src/app/admin/(dashboard)/layout.tsx | جلسة الموظف والبيانات والهيكل الإداري |
| src/lib/admin/session.ts | active profile + دور وصلاحيات؛ requirePermission |
| src/lib/cms/admin-loader.ts وsrc/lib/admin/data-access.ts | تحميل بيانات الإدارة وحصر البيانات المسلسلة حسب الصلاحيات |
| src/app/admin/actions | كتابة المحتوى والإعدادات والمستخدمين والإشراف والمصادقة |
| src/context/admin-store.tsx | مسودات وحالة الإدارة فوق البيانات القادمة من Supabase |
| src/lib/supabase | browser/server/service، والوسائط |
| src/lib/community وsrc/app/community | ملفات الأعضاء والخلاصة والتفاعلات والمشاريع والإشعارات |
| supabase/migrations | العقد المرجعي؛ 9 ملفات SQL قائمة لا تُعاد على Production |
| tests | محولات CMS والمجتمع، استعلامات HTTP محاكية، عقد SQL، وحواجز الإدارة |
| scripts/build.mjs وscripts/start.mjs | بناء وتغليف standalone وتحميل البيئة على Windows |

يوجد مكوّن ImageUpload قديم للمعاينة فقط في بعض المحررات؛ مكتبة الوسائط ترفع فعلًا. لا تعمم سلوك أحدهما على الآخر. لا يوجد Prisma مستخدم ولا NextAuth مستخدم؛ أزيل القالب غير المستخدم في إصلاح الاستقرار.

## 4. قرارات سارية وملاحظات استئناف

- D-85/D-86: Supabase مصدر بيانات CMS، والقراءة العامة بعميل منفصل بلا صلاحيات الإدارة. D-45/D-47 المتعلقان بـlocalStorage ومزامنة التبويبات يصفان مرحلة Mock وقد استُبدلا.
- D-79: مجموعة (dashboard) لا تظهر في عنوان URL؛ المسار الفعلي للإشراف /admin/community.
- D-87: فصل publish_status عن operational_status وحالة الموعد؛ لا تدمجها في قاعدة البيانات.
- D-89: shortName حقل محلي غير مخزن بعقد courses؛ لا تدّعِ استمراره بعد refresh.
- D-100: community_profiles لهوية العضو، وprofiles لفريق الإدارة. وجود auth.users وحده لا يمنح صلاحية مالك.

| القرار | التاريخ | التثبيت الحالي وسببه |
|---|---|---|
| D-101 | 2026-09-12 | أوامر البناء والتشغيل عبر Node؛ لا حاجة إلى Bash أو cp -r أو prisma generate. ignoreBuildErrors أزيل، وفحص TypeScript جزء من البناء. توثيق إصلاح 3d5ef34. |
| D-102 | 2026-09-12 | تثبيت حدود الأمان: البيانات المسلسلة للإدارة مقيدة بالصلاحيات، الموظف active فقط، تفويض الأدوار والنشر محميان خادميًا. لا نساوي ذلك بتصحيح جميع سياسات RLS. |
| D-103 | 2026-09-12 | حفظ الرئيسية ينعكس بعد الطلب/إعادة التحميل، لا فورًا داخل تبويب الزائر المفتوح؛ اليدوي بلا موعد يرجع تلقائيًا. مثبت بتجربة CMS فعلية. |
| D-104 | 2026-09-12 | فصل زمن الأدلة: مراجعة الاستقرار تمت بلا حسابات Supabase؛ التجربة اللاحقة حصلت بعد إعداد المالك للبيئة. الاختبار الحي اللاحق محدود بـCMS ولا يغطي المجتمع أو البريد. |
| D-105 | 2026-09-12 | تأكيد حظر إعادة SQL على Production؛ إصلاحات سياسة قاعدة البيانات تحتاج نطاقًا مستقلًا مأذونًا، حتى لو أوصت وثيقة تاريخية بـrepair. |
| D-106 | 2026-09-12 | تحديث PRD/Memory وملفات التشغيل بطلب المالك؛ حفظ النسخ القديمة في docs/history لاستبقاء القرارات دون استمرار تعليمات Mock كحالة حالية. |

## 5. التشغيل والفحص

متطلبات package.json: Node >=22 وBun >=1.3. البيئة المختبرة: Node 24.18.0 وBun 1.4.0. Supabase JS 2.115.0 وSSR 0.12.6 مثبتان، وNext المختبر 16.3.4. بقية نطاقات الحزم ونسخها الدقيقة في package.json وbun.lock.

~~~cmd
bun install --frozen-lockfile
bun run typecheck
bun run lint
bun run test
bun run build
bun run start
~~~

ملف .env.local بجوار package.json؛ الأسماء: NEXT_PUBLIC_SUPABASE_URL وNEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY وSUPABASE_SECRET_KEY. جهّز القيم قبل البناء؛ متغيرات NEXT_PUBLIC_ مضمّنة في المتصفح. start.mjs يحمل البيئة للخادم أيضًا. تغيير محتوى CMS لا يحتاج build، وتغيير المتغيرات العامة يحتاجه. لا يوجد CI workflow في .github داخل النسخة المفحوصة.

## 6. المزالق والعمل المتبقي

1. الصورة المختارة في بعض المحررات معاينة blob فقط؛ لا تعاملها كملف محفوظ. توصيلها بمكتبة الوسائط لم يُنفذ في جلسة التوثيق.
2. الحفظ الشامل للرئيسية يعيد كتابة أقسام وجداول متعددة، وقد يعيد إنشاء معرّفات عناصر القوائم؛ ليس transaction. استعادة المحتوى لا تعني تطابق timestamps أو IDs الداخلية.
3. قواعد RLS القائمة للمجتمع: إمكان تحديث أعمدة إشعار أكثر من read_at، وبعض شروط تعديل الوسائط/نشاط العضو غير مكتملة. أصلحها فقط بتغيير قاعدة منفصل مأذون ومختبر.
4. Incoming blocks غير مرئية بقائمة الحجب الذاتية؛ الحجب المتبادل للتفاعلات يقع في SQL. ملفات Storage العامة تظل متاحة بالرابط بعد إخفاء المنشور.
5. لا generated Supabase Database types؛ الاختبارات الحالية لا تفحص كل تركيب query ديناميكي ولا تثبت تطابق Production كله.
6. اختبارات مجتمع بحسابين، وصول الدعوات بالبريد، مسارات الفشل المتوسطة، WCAG شامل، الحمل، النسخ الاحتياطي والاستعادة: غير معتمدة بنتائج حديثة شاملة.
7. بيانات siteConfig الافتراضية والدومين وSEO وإعدادات SMTP/Redirect URLs تحتاج مطابقة بيئة النشر الفعلية. لم يُختبر موقع مستضاف بعيد في تجربة CMS المحلية.
8. middleware.ts باقٍ مع تحذير إهمال Next؛ noImplicitAny=false وskipLibCheck=true وreactStrictMode=false قائمة وليست تغييرات جديدة.
9. تعليق Mock في الكود قديم أحيانًا؛ اقرأ التنفيذ الفعلي. بعض المعاينات فعلًا Mock، فلا تحذف التنبيه دون إكمال الربط.

## 7. سجل الجلسات

السجل السابق كاملًا في [الأرشيف](docs/history/memory-before-2026-09-12.md)، وتفاصيل التنفيذ في [worklog.md](worklog.md).

### 2026-09-12 — إغلاق الاستقرار والتشغيل المحلي

- دمج 45 ملفًا في commit الاستقرار 3d5ef34 ودفعه إلى main؛ إصلاحات البناء والأمان والتوافق موثقة في STABILITY_AUDIT.md.
- TypeScript وESLint والبناء نجحت، 70 اختبارًا/214 assertion؛ فحص المصدر وتاريخ Git بلا أسرار، وbun audit بلا advisories وقت المراجعة.
- سلّمت نسخة Windows، ثم أكد المالك تشغيل المشروع وإعداد مفاتيح البيئة محليًا. لا تُحفظ قيمها هنا.

### 2026-09-12 — اختبار حفظ الرئيسية بتفويض المالك

- متصفح إدارة بجلسة اختبار مؤقتة في الذاكرة، ومتصفح زائر مجهول منفصل؛ لم يتغير دور أو كلمة مرور، ولم تُرسل رسائل بريد. أُنهيت جلسة الاختبار وحدها.
- الحالة الأصلية: وضع يدوي لدورة الإضاءة الاستوديوهية بلا موعد؛ العرض العام يرجع إلى ورشة أساسيات التصوير.
- تغيير عنوان Hero مؤقتًا واختيار موعد 2026-09-21؛ الحفظ نجح خلال 9362ms في العينة، وظهرت البيانات بعد reload خلال 824ms. هذه قياسات عينة وليست SLA.
- تحقق حفظ القاعدة وإعادة فتح الإدارة وغياب التحديث الذاتي في تبويب الزائر؛ لا أخطاء JavaScript ملتقطة.
- استعادة العنوان والاختيارات الأصلية، ومقارنة محتوى 14 جدولًا للرئيسية بنجاح بعد استبعاد timestamps ومعرّفات الصفوف المعاد إنشاؤها. لا تغييرات schema/migrations.

### 2026-09-12 — تحديث وثائق المشروع بطلب المالك

- إعداد PRD حالي وذاكرة تشغيل مختصرة، وأرشفة النسخ القديمة دون فقد سجل القرارات، وتصحيح README وبيانات الحالة في الوثائق المرتبطة.
- إعداد PROJECT_REPORT.md وتوثيق نتيجة CMS وحجم التحقق وحدود الأمان والوسائط والمدفوعات.
- التعديلات توثيقية فقط؛ لا كود تطبيق ولا dependency ولا SQL ولا بيانات Production جديدة. لم يُعد تشغيل اختبار الكتابة الحي أو بناء يمس الخادم الجاري لأجل Markdown.
- تحقق سلامة نسختي الأرشيف وروابط الوثائق والأسوار البرمجية وUTF-8؛ فحص Gitleaks لمجموعة الوثائق المعدلة بلا أسرار. أضيف سجل نتائج CMS المنقح في docs/evidence.

### تجهيز نطاق النشر — baytalmosawer.net

- المالك طلب نشر المشروع كاملًا على Hostinger Business ثم أكد إبقاء Supabase الحالية وحدد الدومين baytalmosawer.net.
- D-107: اعتماد https://baytalmosawer.net في src/data/site.ts وروابط خريطة الموقع في public/robots.txt. تعديل الإعداد العام المجمد ضروري لطلب النشر؛ لا تغيير للبريد أو بيانات التواصل بافتراض أنها تستخدم النطاق نفسه.
- لا وصول متصل إلى hPanel حتى الآن؛ متابعة إعداد Deploy Web App وربط GitHub والبيئة تتم مع المالك. إعدادات DNS وTLS وSupabase Auth لم تتغير بعد، ولا migrations أو نقل قاعدة.
- إعدادات hPanel الفعلية لا توفر Bun؛ استُكمل تجهيز npm كما هو موثق أدناه. لا يُعد تعديل الدومين في Git نشرًا ناجحًا أو اعتمادًا لأمان Community.

### توافق مدير الحزم مع Hostinger

- D-108: استخدام npm في Hostinger؛ إضافة package-lock.json مع إبقاء bun.lock للتطوير والاختبارات. إعدادات النشر: Next.js، main، Node 22.x، الجذر ./، الأمر npm run build، الإخراج .next.
- ملف npm يثبت 683 حزمة ويتضمن حزم Linux الاختيارية. جميع الاعتماديات المباشرة تطابق إصدارات bun.lock؛ توجد خمسة إصدارات متداخلة خاصة بمسار WASM الاختياري حُلّت بحسب قيود npm، لذلك لا يُدّعى تطابق شجرتي الحزم بالكامل.
- كل حزمة لها رابط registry.npmjs.org وبصمة integrity. نُقلت البصمات المتاحة من bun.lock واستُكملت بيانات الحزم الأخرى من سجل npm الرسمي.
- جرى التحقق في نسخة منفصلة دون بيئة Production أو تغيير الخادم المحلي الجاري. الاختبارات لا تعيد migrations ولا تكتب بيانات الإنتاج. نجاح هذه الفحوصات لا يثبت النشر على هوستنجر؛ سجل البناء المستضاف وفحص الموقع العام ما زالا مطلوبين.
- النتائج: تثبيت npm نظيف وnpm ci بملف القفل النهائي PASS؛ TypeScript وESLint PASS؛ 70 اختبارًا/214 assertion PASS؛ npm run build PASS مع توليد 67/67 وتجهيز standalone. npm audit: صفر ثغرات معلنة؛ Gitleaks لملف القفل بلا أسرار. بيئة الفحص Windows وNode 24.18.0 وnpm 11.16.0؛ تشغيل Linux/Node 22 في Hostinger ما زال يُتحقق منه عند النشر. بقي تحذير middleware القديم وتحذير npm بشأن postinstall لحزمة unrs-resolver دون فشل الفحوصات.

### طلب تجهيز ميسر — عمل جارٍ، غير منشور

- أثبتت صورة hPanel اكتمال نشر fd139968 على Node 22.x. اختُبرت الرئيسية والدورات والمجتمع ودخول الإدارة وrobots عبر HTTPS: 200؛ الرئيسية والدخول بلا pageerror أو صور مكسورة في المتصفح. لا اختبار دخول أو حفظ جديد على النسخة المستضافة بعد.
- D-109: المالك طلب استيفاء متطلبات تفعيل ميسر وتجهيز إدخال المفاتيح لاحقًا. راجع docs/MOYASAR_READINESS.md قبل الاستئناف. ليست هذه موافقة ضمنية على تغيير سياسات الاسترجاع أو اختلاق بيانات المنشأة أو إجراء دفعات حقيقية.
- الموقع المنشور يعرض هاتفًا تجريبيًا، وسياسة الاسترجاع تحتاج إزالة غموض 48–72 ساعة واعتماد المالك. ينتظر العمل بيانات المنشأة والتواصل واعتماد الأسعار/الضريبة/المواعيد والسياسات، واختيار موضع المفاتيح وطريقة السداد (كامل/عربون). الأسئلة أُرسلت؛ لا إجابات حتى هذه النقطة.
- أضيفت مسودة خادمية src/lib/payments/moyasar.ts واختباراتها للتحقق من API والمبلغ والعملة والطلب والبيئة والأخطاء المنقحة. ليست Checkout أو Webhook أو نظام طلبات. لا تحصيل مفعل ولا تعديل قاعدة ولا تغيير مفاتيح.
- TypeScript وESLint ناجحان؛ 77 اختبارًا/259 assertion ناجحة. لم يُعد بناء الإنتاج أو دفع هذه المسودة؛ استكمل التكامل والاختبارات قبل commit/push. آخر بناء منشور سابق موثق أعلاه. لا تُعرض هذه المسودة على أنها جاهزية دفع مكتملة.
- استُكملت واجهة /admin/settings/payments كصفحة خادمية + مكوّن عميل: إعدادات تجارية ومفاتيح مشفّرة لكل مزود وبيئة، بحفظ واختبار اتصال قراءة فقط وحذف، محصورة بحساب المالك. المفاتيح لا تُعاد إلى المتصفح. TypeScript وESLint و77 اختبارًا PASS. ما زالت مسودة غير مدفوعة، والترحيل غير مطبق وPAYMENTS_ENCRYPTION_KEY غير مضبوط، فالتحصيل مغلق.
- D-110: وسائل التواصل الاجتماعي صارت قائمة واحدة في جدول social_links تُدار من «بيانات التواصل»، بعد اكتشاف أن الاشتقاق العام كان يقارن uuid صف الفوتر باسم المنصة فتسقط كل الروابط. أربع عشرة منصة بأيقوناتها، والرابط يُطبَّع على الخادم ويرفض غير http/https.
- D-111: بيانات المنشأة القانونية والضريبية في صفحة مستقلة /admin/settings/company بإجراء للمالك وحده يكتب أعمدة الهوية فقط؛ صفحة تجهيز الدفع بقيت للسداد والعربون والسياسات والمفاتيح. ترحيلا social_links وتجهيز الدفع ما زالا غير مطبقين على أي قاعدة، وPAYMENTS_ENCRYPTION_KEY غير مضبوط.
- D-112: أربعة عيوب RLS في المجتمع صُحّحت في ترحيل 20260912140000 غير مطبق بعد. أخطرها أن community_block_between كانت تمنع التفاعل مع محتوى النفس، فتعذّر على صاحب المنشور الرد على تعليقات منشوره. البقية: منح تعديل الإشعارات صار على عمود read_at وحده، وتعديل صفوف الوسائط صار ملزَمًا بمجلد المالك. لا تُطبَّق على Production قبل اختبار حي بحسابين.
- D-113: لا generated Database types بعد. البديل الحالي فحص ثابت في tests/supabase-column-contract.test.ts يطابق أسماء الأعمدة الحرفية مع ملفات SQL؛ استبدله بالأنواع المولّدة عند توفر وصول supabase CLI، ولا تعامله كضمان أنواع.
- D-114: حذف منشور أو مشروع مجتمع صار يزيل ملفاته من community-media في المسارات الثلاثة (عضو، مشروع، إشراف). الترتيب إلزامي: قراءة storage_path قبل حذف الصف لأن الوسائط تختفي بـcascade. إخفاء المنشور ما زال يترك الصورة مخدومة لأن الـbucket عام؛ علاجه يحتاج قرار bucket خاص وروابط موقّعة.
- D-115: كل جداول المجتمع الاثني عشر على الإنتاج فارغة تمامًا (صفر صفوف) بتاريخ 2026-09-12. المجتمع منشور ولم يسجل فيه عضو واحد. هذا يخفض خطر تطبيق تصحيحات RLS إلى أدنى حد، ويجعل الاختبار الحي بحسابين ممكنًا دون تلويث بيانات حقيقية.
- D-116: وُلّدت src/types/database.ts من القاعدة الحقيقية (2497 سطرًا). لم تُربط بالعملاء بعد لأن الربط يكشف 62 خطأ نوع، 36 منها تزول بتطبيق الترحيلات الثلاثة و26 إصلاحات enum حقيقية. اربطها بعد التطبيق ثم أصلح الـ26 ولا تُبقِ الشجرة حمراء بينهما.
- D-117: تطبيق أي ترحيل جديد على الإنتاج محجوب بأمرين معًا: كلمة مرور القاعدة لـsupabase link (يضبطها المالك بنفسه في متغير بيئة، لا تُلصق في المحادثة)، وإذن صريح بـmigration repair --status applied للنسخ الثلاث 20260910090000 و20260910091000 و20260910092000 لتصحيح انحراف السجل.
- D-118: نُفّذ migration repair --status applied بإذن المالك لخمس نسخ (20260909090000، 20260909091000، 20260910090000، 20260910091000، 20260910092000) لا ثلاث كما افترضت وثيقة التسوية. تسع ترحيلات صارت local=remote.
- D-119: db push مسدود رغم الإصلاح بسبب 13 مدخلًا remote-only. ادعاء MIGRATION_RECONCILIATION.md أن هذه المدخلات لا تعيق شيئًا خاطئ ويجب تصحيحه. الحل الذي يقترحه CLI (repair --status reverted) ضمن ممنوعات المالك. البديل المتاح: تنفيذ ملفات الترحيل بـsupabase db query --linked ثم تسجيلها بـrepair --status applied — لا shadow migrations ولا INSERT يدوي.
- D-120: الترحيلات الثلاثة مطبَّقة ومسجَّلة على الإنتاج. migration list: 12 متزامنًا وصفر معلّق. تأكدت التصحيحات الأربعة بالاستعلام المباشر عن الدالة والمنح والسياستين.
- D-121: src/types/database.ts مولّد ومربوط بالعملاء الثلاثة. أعد توليده بعد أي ترحيل: supabase gen types typescript --project-id rnzdleotnxznkqfrcwfa --schema public > src/types/database.ts.
- D-122: قيم التعدادات تمر عبر src/lib/cms/enums.ts وحده. لا تضف مصفوفة تحقق يدوية جديدة؛ القوائم يحرس اكتمالها tests/db-enums.test.ts مقابل الملف المولّد.
- D-123: db push يبقى مسدودًا بـ13 مدخل remote-only. مسار أي ترحيل قادم: supabase db query --linked -f <ملف> ثم migration repair --status applied <نسخة>. حاجز auto يمنع تنفيذ DDL من الجلسة، فينفذه المالك.
- D-124: تصحيحات RLS الأربعة مؤكدة سلوكيًا باختبار حي بحسابين على الإنتاج (17/17)، لا على مستوى الكائنات فقط. أُعيدت القاعدة فارغة تمامًا بعده. السيناريو محفوظ في supabase/tests/community_rls_live.mjs ويُشغَّل بـBMNET_LIVE_RLS_TEST=1 فقط.
- D-125: سياسات storage.objects للمجتمع مؤكدة حيًا (24/24 في السيناريو الحي). ملاحظة سلوكية: حذف ملف لا يملكه العضو يعيد قائمة محذوفات فارغة بلا خطأ — لا تعتمد غياب الخطأ دليلَ نجاح، افحص طول data.
- D-126: ثغرة «إخفاء المنشور لا يخفي صورته» موثقة بتأكيد معكوس في السيناريو الحي، مكتوب ليفشل عمدًا عند معالجتها.
- D-127: خلاصة المجتمع لم تعمل قط على الإنتاج بسبب PGRST201: تضمين community_profiles بلا تسمية مفتاح، وبين الجدولين ثلاث علاقات. هذا سبب صفر أعضاء، لا البريد. أُصلحت أربعة مواضع ويحرسها tests/postgrest-embeds.test.ts. القاعدة: كل تضمين لـcommunity_profiles يسمّي مفتاحه صراحة.
- D-128: مشروع Supabase يستخدم مزود البريد الافتراضي: رسالة واحدة ثم 429 over_email_send_rate_limit. لا يصلح لإطلاق المجتمع؛ يلزم SMTP مخصص. وصول الرسالة إلى صندوق فعلي غير مُتحقق منه بعد.
- D-129: مفتاح service_role القديم (JWT) انكشف في سجل جلسة 2026-09-12 عبر supabase projects api-keys. لا تشغّل هذا الأمر. يلزم تعطيل Legacy API keys من لوحة Supabase.
- D-130: تدقيق أمني شامل 2026-09-12. السليم: 57/57 جدولًا بـRLS، 14 جدولًا حساسًا محجوبًا عن anon بالاختبار الفعلي، كل إجراء خادمي مُبوَّب، صفر أسرار في حزمة المتصفح، صفر ثغرات npm. المعلّق كله إعدادات لوحة Supabase لا كود.
- D-131: النماذج العامة صار لها حدّ معدل في ذاكرة العملية (5 لكل 10 دقائق). ليست حدًا موزعًا وتُفقد عند إعادة التشغيل؛ استبدلها بحدّ في القاعدة إن صار النشر متعدد النسخ.
- D-132: لا تستخدم كلمات مرور يرسلها المالك للدخول. التدقيق يُجرى بوصول الخدمة من .env.local وهو أعمق وأأمن.
- D-133: ImageUpload صار يرفع فعلًا إلى bm-media في المحررات التسعة (11 استخدامًا) بعد أن كان معاينة blob. القاعدة: قيمة الحقل لا تتغير إلا بعد نجاح الرفع. uploadMediaAction صار يعيد url وstoragePath لا المعرّف وحده. لكل محرر مجلده عبر prop folder، ويحرسه tests/image-upload-contract.test.ts.
- D-134: فجوتا V1 في المجتمع أُغلقتا: صفحة المحفوظات وصفحة المحجوبين مع MemberNav. الأهم أن الحجب كان بابًا لا رجعة منه — منشورات المحجوب مُخفاة وزر فك الحجب داخل بطاقته وحدها. قاعدة عامة يحرسها tests/community-reachability.test.ts: كل إجراء يغيّر حالة العضو يحتاج مسار وصول.
- D-135: loadBlockedMembers يقرأ الملفات بعميل الخدمة عمدًا؛ سياسة العرض العام تُخفي العضو الموقوف ومن حجبه يجب أن يفك عنه مهما كانت حالته.
- D-136: حقن كوكي جلسة في المتصفح يرفضه حاجز الأمان (يشبه اختطاف جلسة). معاينة صفحات الأعضاء بحساب فعلي تبقى على المالك.
- D-137: حذف كائن من bm-media يعمل، لكن CDN يخدم النسخة المخزّنة حتى ساعة (cache-control max-age=3600، cf-cache-status: HIT بعد الحذف، وكاسر التخزين يعيد 400). الحذف ليس فوريًا من منظور الزائر؛ المعالجة الجذرية هي نفسها معالجة ثغرة إخفاء الصور.
- D-138: أدوات الفحص الثابتة تعطي إنذارات كاذبة متكررة — insert().select() تبدو استعلامًا بلا حدّ، والتسمية الضمنية داخل label تبدو حقلًا بلا تسمية، وروابط النص القصيرة تبدو أهدافًا صغيرة. تحقق بالسياق قبل تسجيل أي نتيجة.
- D-139: اختبار يفحص قائمة ملفات مكتوبة يدويًا يُخفي الفجوة التي كُتب لأجلها. امسح كل الملفات وقارن عدد الاستخدامات بعدد الصفات.
- D-140: التطبيق لا يعتمد على أي مفتاح Supabase قديم — أُثبت بالكود والحزمة المبنية واختبار الفعالية. المفاتيح القديمة (anon وservice_role) ما زالت فعّالة وتعطيلها آمن. المفتاح السري المستخدم هو baytalmosawer لا default؛ لا تحذف الآخر بلا تحقق.
- D-141: الرابط الموقّع في Supabase Storage يعود بـcache-control: null — لا تخزين CDN. هذا الثمن المقاس لتحويل community-media إلى خاص، وهو القياس الحاسم في docs/DECISION_community_media_bucket.md.
- D-142: علة صامتة — إنشاء جدول بسياسات RLS بلا GRANT على مستوى الجدول يجعل السياسات غير قابلة للبلوغ (42501 قبل التقييم). وقعت في social_links فمنعت ظهور روابط السوشيال عامًا، وأخفاها أن الجدول ضمن OPTIONAL_TABLES وأن الإدارة تقرأ بعميل الخدمة. أي جدول جديد بسياسة anon يحتاج grant صريحًا مثل footer_links.
- D-143: حارس اختبار قد يمرّ فارغًا إن لم يعثر على ما يفحصه. أضف دائمًا تأكيدًا صريحًا على عدد العناصر المكتشفة قبل الحكم، وأثبت فعالية الحارس بكسر الشرط مؤقتًا.
- D-144: Medium #9 مغلق بـbucket خاص + مسار خادمي /community/media يفحص الظهور عند كل طلب. اختير على الروابط الموقّعة لأن الإخفاء يسري فورًا لا بعد انتهاء مدة، ولأن نقطة التحويل واحدة فلم يتغير أي مستدعٍ. مذكرة القرار السابقة كانت ترجّح الروابط الموقّعة؛ هذا الحل أبسط وأقوى إشرافيًا.
- D-145: منطق ظهور الوسائط في src/lib/community/media-access.ts وحده. أي ميزة جديدة تعرض وسائط مجتمع تمر به، ولا تبني رابط تخزين مباشرًا.
- D-146: نطاق الـmiddleware `/admin` و`/community` فقط. توسيعه يعيد رحلة مصادقة على كل صفحة عامة بلا فائدة — الصفحات العامة ساكنة ولا تقرأ جلسة. اختبار `tests/public-first-auth.test.ts` يحرس هذا.
- D-147: **لا تقرأ المسار أو الاستعلام بخطاف داخل مكوّن عميل تحت Suspense.** `useSearchParams`/`usePathname` يؤجّلان الحدّ فيبقى على هيكل التحميل — أخفى خلاصة المجتمع كليًا ولم يكشفه أي اختبار ساكن. مرّر الوجهة خاصيةً من الخادم. وهو أيضًا سبب عودة نموذجي الدخول والتسجيل إلى HTML.
- D-148: المصادقة ليست صلاحية إدارة. `loginAction` يفحص ملف إدارة نشطًا بعد `signInWithPassword` ويغلق الجلسة عند الرفض — أي عضو مجتمع يملك حسابًا صالحًا على نفس مشروع Supabase.
- D-149: روابط الدخول/التسجيل تُبنى من `src/lib/community/auth-links.ts` وحده لتمر بحارس open-redirect نفسه ولا تُسقط وجهة العودة.
- D-150: **لا تقرأ الجلسة على الخادم في الشريط أو التذييل.** كلاهما في root layout، فالقراءة تُخرج كل صفحة عامة من التوليد الساكن. الحالة تُقرأ في المتصفح (`useViewerSession`) والخادم يرسم حالة الزائر.
- D-151: أي عنصر واجهة يتبدل نصه بعد الإرطاب يحتاج حجز عرض يسع أطول نص — وإلا زحف ما حوله. قِيس 10px عند حجز أضيق من «تسجيل الدخول».
- D-152: `/account` موجّه خادمي يقرر وجهة «حسابي» (موظف ← اللوحة، عضو ← ملفه). سببه أن تمييز الموظف يحتاج `select` على `profiles` لدور `authenticated` — وهي غير ممنوحة عمدًا ولا تُمنح لأجل وسم في قائمة.
