# PRE-AUDIT CHECKPOINT — بيت المصور (bmnet)

آخر تحديث: 2026-09-09

## حالة الكود والمزامنة
- **آخر commit للكود: `498bc60`** — `fix(media): allow bm-media public URLs in public image rendering (safeImage)`
- الالتزامات الفعالة فوق أساس `origin/main` السابق (`cd29188`) — وقت كتابة هذا الملف هما محليان بانتظار الدفع:
  - `4d67bee` — إصلاح Publish/Unpublish
  - `498bc60` — إصلاح صور Supabase Media
- الـ working tree: نظيف

## الإصلاحات المثبتة في هذا الـ checkpoint
### 1) إصلاح Publish/Unpublish (`4d67bee`)
صفحات التفاصيل العامة (الدورات / مسارات التعلم / المدونة) كانت تعرض البيانات المزروعة (Seed) دائمًا، فإلغاء نشر عنصر لا يخفيه من الموقع العام (soft-200). الإصلاح: قاعدة البيانات مصدر الحقيقة للعرض العام — `notFound()` عند غياب العنصر غير المنشور، مع تسامح موثق عند فشل القاعدة (D-86)، وmetadata مستمدة من القاعدة. إلغاء النشر يُرجع الآن 404 حقيقيًا فورًا.

### 2) إصلاح صور Supabase Media (`498bc60`)
دالة `safeImage` كانت ترفض روابط `bm-media` فتسقط صور الوسائط بصمت (fallback)، و`next.config` بلا `remotePatterns` فمُحسِّن الصور يرجع 400. الإصلاح: قبول روابط وسائط bm-media في العرض العام + إضافة `remotePatterns` لـ `*.supabase.co` — الصور المرفوعة من Media Manager تُعرض الآن فعليًا في الصفحات العامة.

## نتائج الاختبار العملي E2E عبر لوحة الإدارة (/admin)
- **CMS LIVE EDIT STATUS: PASS**
- كل دورات التعديل (اسم/وصف/صورة/نشر/إلغاء نشر/القادمة/الوسائط) تعمل من الواجهة بانعكاس على الموقع العام خلال ثوانٍ عبر `revalidatePath('/', 'layout')` — دون بيانات قديمة، ودون الحاجة لإعادة نشر.
- البيانات الاختبارية استُعادت/حُذفت بالكامل (لا تلوث في القاعدة أو التخزين).
- Quality gates بعد الإصلاحين: lint ✓ / tsc 0 ✓ / bun test 29/29 ✓ / build 58 صفحة ✓ / standalone runtime ✓

## ⚠️ تنبيه — غير مكتمل حاليًا (قيد موثق، ليس bug حاجب للإطلاق)
- **Course Image Picker داخل المحرر غير مكتمل:** حقل صورة الدورة في شاشة تحرير الدورة يعمل بشكل mock (رفع محلي فقط) ولا يوفر منتقي وسائط مدمجًا؛ ربط الصورة بالدورة يتم حاليًا عبر مسار الوسائط/القاعدة. القيد موثق بالكود، والعرض العام للصور يعمل (بعد إصلاح `498bc60`).

## شرط بدء الـ Deep Audit
لا يبدأ الـ Deep Audit قبل: دفع الالتزامات أعلاه إلى `origin/main`، وتحقق `HEAD == origin/main`، وworking tree نظيف.
