# تقرير المشروع — بيت المصور

> المرجع الهندسي الحالي. آخر تحقق: 2026-09-13 (المصادقة: عام أولًا).
> كل رقم هنا مقيس من الكود أو قاعدة الإنتاج وقت الكتابة، لا منقول من تقرير سابق.
> التصنيفات: **PRODUCTION NOW** · **READY IN PR** · **VERIFIED** · **NOT VERIFIED** · **OWNER ACTION** · **DEFERRED**

---

## 1. الحالة العامة

المشروع موقع مركز تدريب تصوير في جدة: موقع عام، لوحة تحكم بصلاحيات، ومجتمع مصورين. Next.js 16 App Router وSupabase (Postgres/Auth/Storage). عربي RTL.

الموقع منشور ويعمل. لوحة التحكم كاملة وعقد القاعدة متين. **مجتمع المصورين كان معطلًا تمامًا منذ النشر** لعلة استعلام، أُصلحت ودُمجت في `main` وطُبّقت ترحيلاتها على الإنتاج.

الكود جاهز للنشر. الحاجز الوحيد المتبقي على إطلاق المجتمع هو **SMTP مخصص** — إجراء بيد المالك.

---

## 2. حالة Git

| البند | القيمة |
|---|---|
| Pull Request [#1](https://github.com/atharz1980-oss/bmnet/pull/1) | **مدموج** |
| بعد الدمج | إغلاق Medium #9 · مصادقة «عام أولًا» |
| المزامنة | `HEAD == origin/main`، شجرة العمل نظيفة |

---

## 2.1 المصادقة — عام أولًا

الموقع مفتوح للتصفح؛ الدخول يُطلب عند الفعل الذي يحتاجه وحده.

| الطبقة | النطاق |
|---|---|
| Middleware | `/admin` و`/community` فقط — لا صفحة عامة تمر به. يحوّل عند `/admin` بلا جلسة وحده |
| layout اللوحة | حاجز ثانٍ: ملف إدارة نشط + دور، وإلا `/admin/login?next=…` |
| `loginAction` | المصادقة لا تكفي — يُفحص ملف الإدارة، والرفض برسالة صريحة مع إغلاق الجلسة |
| صفحات أعضاء المجتمع | كلٌّ تحرس نفسها وتوجّه إلى `/community/login?next=<مسارها>` |
| الأفعال (إعجاب، تعليق، حفظ، متابعة، بلاغ) | الزائر يُؤخذ إلى الدخول ويعود إلى نفس المنشور أو الملف |
| RLS | بلا تغيير — الصفحات العامة تقرأ ما يُقصد نشره فقط |

وجهة العودة تُبنى من `src/lib/community/auth-links.ts` وحده فتمر بحارس open-redirect نفسه. **لا تُقرأ بخطاف داخل مكوّن عميل**: `useSearchParams`/`usePathname` يؤجّلان حدّ Suspense المحيط فتختفي الخلاصة كليًا (وقع وأُصلح — `worklog.md` مهمة 42).

**لا يوجد تسجيل في دورة بعد**: زر الحجز يفتح واتساب، والتسجيل والدفع مؤجلان بأمر المالك — فلا بوابة دخول عليه، ووضعها كان سيحاصر مسار التحويل الوحيد العامل.

تحقق حي: `supabase/tests/public_first_live.mjs` — 44/44.

---

## 3. الأرقام الفعلية

مقيسة بتاريخ 2026-09-12 بعد آخر إصلاح.

### الكود

| القياس | القيمة | ملاحظة |
|---|---|---|
| ملفات TS/TSX | 292 | |
| أسطر `src` | 48,235 | منها 2,654 مولّدة آليًا (`types/database.ts`) — المكتوب يدويًا 45,581 |
| صفحات `page.tsx` | 61 | |
| مسارات API | 2 | `api-feed` و`api-comments` |
| إجراءات خادمية | 82 | موزعة على 14 ملف `"use server"` |
| ملفات اختبار | 15 | |
| ترحيلات SQL | 12 | |

### قاعدة الإنتاج

| القياس | القيمة |
|---|---|
| جداول `public` | 57 |
| سياسات RLS في `public` | 237 |
| سياسات على `storage.objects` | 8 |
| صفوف سجل الترحيلات | 25 (12 متزامنة + 13 مدخلًا تاريخيًا) |
| الدورات | 10 |
| المواعيد | 8 |
| المقالات | 6 |
| المدربون | 3 |
| المسارات | 2 |
| صفوف الوسائط | 14 |
| **أعضاء المجتمع** | **0** |
| رسائل التواصل | 0 |
| طلبات الشركات | 0 |

> **فرق عن تقرير سابق:** عدد الاختبارات 180 لا 185 (ملفات الاختبار 15 كما هي). السبب أن ثلاثة اختبارات ثابتة حلّت محل تسعة اختبارات كانت تفحص قائمة ملفات مكتوبة يدويًا — القائمة نفسها كانت تُخفي فجوة حقيقية. عدد أقل بتغطية أوسع.

---

## 4. ما في الإنتاج الآن مقابل ما في الـPR

### PRODUCTION NOW

الموقع العام بكل صفحاته · لوحة التحكم بـ16 وحدة صلاحيات وخمسة أدوار · مكتبة الوسائط · الصفحات القانونية · نماذج التواصل وطلبات الشركات · **مخطط المجتمع وسياساته في القاعدة**.

الترحيلات الثلاثة الجديدة **مطبَّقة ومسجَّلة على القاعدة بالفعل** (`commerce_settings`، `payment_credentials`، `social_links`، وتصحيحات RLS). القاعدة متقدمة على الكود المنشور، وهذا آمن لأن الإضافات لا يستخدمها الكود القديم.

### READY IN PR — لم يصل الموقع الحي

| الميزة | الوصف |
|---|---|
| إصلاح خلاصة المجتمع | العلة التي عطّلت المجتمع بالكامل |
| صفحتا المحفوظات والمحجوبين | + شريط تنقّل الأعضاء |
| قائمة السوشيال الموحّدة | 14 منصة تُدار من الإعدادات |
| صفحة بيانات المنشأة | الهوية القانونية والضريبية |
| صفحة تجهيز الدفع | السداد والعربون ومفاتيح المزودين |
| رفع الصور الحقيقي | 16 استخدامًا عبر 11 ملفًا |
| تنظيف التخزين عند الحذف | منشورات ومشاريع |
| حدّ معدل النماذج العامة | 5 لكل 10 دقائق |
| أنواع القاعدة المولّدة | مربوطة بالعملاء الثلاثة |

### NOT IMPLEMENTED

نظام تسجيل وطلبات · Checkout ودفع فعلي وWebhook · استيراد Google Reviews · WhatsApp API.

### DEFERRED — بأمر المالك

دردشة · رسائل لحظية · سوق وظائف · مجموعات · قصص · بث · إشعارات فورية · ترتيب بالذكاء الاصطناعي.

---

## 5. العلل المكتشفة وحالتها

| # | العلة | الشدة | الحالة |
|---|---|---|---|
| 1 | خلاصة المجتمع لم تعمل قط (`PGRST201`) | CRITICAL | **FIXED · VERIFIED** — ساكن وحي ومتصفح |
| 2 | تعذّر التفاعل مع محتوى النفس (RLS) | HIGH | **FIXED · APPLIED · VERIFIED** حيًا |
| 3 | تعديل الإشعارات على مستوى الجدول | HIGH | **FIXED · APPLIED · VERIFIED** حيًا |
| 4 | سقوط شرط ملكية مسار التخزين في تعديل الوسائط | HIGH | **FIXED · APPLIED · VERIFIED** حيًا |
| 5 | الحجب باب لا رجعة منه | HIGH | **FIXED** في الـPR — بنيويًا |
| 6 | رفع الصور وهمي في المحررات | HIGH | **FIXED** في الـPR — لم يُختبر عبر واجهة الإدارة |
| 7 | حذف المنشور لا يحذف صورته | MEDIUM | **FIXED** — وتحفّظ CDN سقط مع §8 |
| 8 | النماذج العامة بلا حدّ معدل | MEDIUM | **FIXED** في الـPR |
| 9 | إخفاء المنشور لا يخفي صورته | MEDIUM | **FIXED** — bucket خاص ومسار عرض يفحص عند كل طلب (§8) |

### علل كشفتها المراجعة الذاتية للـPR

| # | العلة | الشدة | الحالة |
|---|---|---|---|
| 10 | خمسة `ImageUpload` في `src/app` بلا `folder`، فالشعارات وصور OG ترتب في `misc` | MEDIUM | **FIXED** |
| 11 | اختبار المجلدات كان يفحص قائمة ملفات ثابتة فأخفى العلة 10 | MEDIUM | **FIXED** — يمسح كل الملفات الآن |
| 12 | `loadBlockedMembers` بلا سقف صفوف | LOW | **FIXED** — سقف 100 |
| 13 | الاشتقاق العام يثق في الرابط المخزَّن بلا إعادة تحقق | LOW | **FIXED** — حارس عرض |
| 14 | نصوص تأكيدات الاختبار الحي تُحسب قبل النتيجة فتطبع رسالة فشل مع PASS | LOW | **FIXED** — دالة `checkRefused` |

---

## 6. نتائج الأمان وRLS

### VERIFIED — بالفحص الفعلي لا بالقراءة

| الفحص | النتيجة |
|---|---|
| RLS مفعّل على جداول `public` | **57 / 57** |
| جداول حساسة اختُبرت بعميل الزائر | **14 / 14 محجوبة** (`42501`) |
| سياسات كتابة بشرط `true` | 0 |
| دوال `SECURITY DEFINER` بلا `search_path` | 0 |
| إجراءات خادمية غير عامة بلا بوابة | 0 |
| منح كتابة لـ`anon` | 2 فقط، كلاهما مقصود ومقيّد بشروط |
| أسرار في حزمة المتصفح | 0 |
| ثغرات `npm audit` | 0 |
| فحص أسرار على كل ملفات الـPR | نظيف |

### اختبار RLS الحي بحسابين — 38 / 38

جميع محاولات التجاوز رُفضت على مستوى القاعدة لا الواجهة:

تعديل أو حذف محتوى عضو آخر · انتحال `author_id` في المنشور والتعليق · قراءة إشعارات أو محفوظات أو قائمة حجب عضو آخر · تعليق حساب عضو آخر أو تعديل ملفه · قراءة منشور أخفاه الإشراف · إحياء منشور مخفى إداريًا · إغلاق بلاغ ذاتيًا · رفع ملف في مجلد عضو آخر أو خارج بادئة `community` · حذف ملف عضو آخر · رفع من زائر مجهول · التفاعل بعد الحجب · متابعة الذات · تغيير حالة الملف الذاتي.

السيناريو في `supabase/tests/community_rls_live.mjs`، ويُشغَّل بـ`BMNET_LIVE_RLS_TEST=1` فقط.

### مؤشرات Supabase الرسمية

- **أمان:** تحذير واحد — `Leaked Password Protection` معطّلة. **OWNER ACTION**.
- **أداء:** 39 تنبيه `Multiple Permissive Policies` — طبيعتها أداء لا أمان (سياسة عامة وأخرى إدارية على الجدول نفسه). لم تُعالج.

---

## 7. نتائج الاختبار بالمتصفح وبناء الإنتاج

نُفّذت على **بناء الإنتاج standalone**، لا على خادم التطوير.

| الفحص | النتيجة |
|---|---|
| مسارات عامة | **22 / 22** ترجع 200 |
| مسارات محمية | **10 / 10** تعيد التوجيه للدخول بلا تسريب محتوى |
| صفحات 404 | 4 / 4 صحيحة |
| `robots.txt` | يحوي Sitemap ويمنع `/admin` |
| `sitemap.xml` | 30 رابطًا، بلا `/admin` وبلا صفحات أعضاء |
| أخطاء وحدة تحكم المتصفح | 0 على المسارات المفحوصة |
| صور مكسورة | 0 من 14 في الرئيسية |
| فيض أفقي على 375px | لا شيء على الصفحات المفحوصة |

### الوصولية — VERIFIED جزئيًا

فُحصت 15 صفحة آليًا على ناتج الخادم: `h1` واحد لكل صفحة · صفر صور بلا `alt` · صفر أزرار بلا اسم متاح · صفر حقول بلا تسمية · `lang="ar"` و`dir="rtl"`.

المكوّنات الجديدة تحمل `aria-label` على الأزرار الأيقونية، و`aria-live` على حالات التقدم، و`role="alert"` على الأخطاء.

> **تصحيحان في أدوات الفحص:** أُبلغ أولًا عن حقل بلا تسمية في `/community/photographers` وعن 16 هدفًا صغيرًا في `/contact`. كلاهما إنذار كاذب: الحقل داخل `<label>` بتسمية ضمنية صحيحة، والأهداف روابط نصية في الفوتر ورابط «تخطَّ إلى المحتوى» بنمط `sr-only`.

**NOT VERIFIED:** قارئ شاشة فعلي · تنقّل لوحة مفاتيح يدوي كامل · تباين ألوان مقيس · مراجعة WCAG 2.1 AA شاملة.

### الأداء

قياس ثلاث مرات لكل مسار على بناء الإنتاج، والقيمة وسيطة:

| المسار | الوسيط |
|---|---|
| الرئيسية | 31ms |
| المدونة | 23ms |
| تواصل | 20ms |
| الدورات | 596ms |
| المجتمع | 555ms |
| المصورون | 511ms |

الصفحات الساكنة فورية، والديناميكية التي تضرب القاعدة بين نصف ثانية وستة أعشار. أول طلب بعد الإقلاع استغرق 7.2 ثانية مرة واحدة — بدء اتصال بارد، لم يتكرر.

لم يُرصد N+1: استعلامات الخلاصة والملف تجلب العلاقات بتضمين واحد. استعلام واحد بلا سقف صُحّح (`loadBlockedMembers`).

**NOT VERIFIED:** اختبار حمل · قياس Core Web Vitals · سلوك تحت تزامن عالٍ.

---

## 8. الوسائط

مسار الرفع مُتحقق منه **تحت بوابة الصلاحية** (تخزين + قاعدة + رابط + حذف): 7 / 8 تأكيدات.

| الفحص | النتيجة |
|---|---|
| الرفع إلى `bm-media` | PASS |
| إنشاء صف `media` | PASS |
| الرابط العام يخدم الملف | PASS |
| نوع المحتوى صورة | PASS |
| محسِّن `next/image` يقبل نطاق Supabase | PASS |
| الرفع المكرر بنفس المسار مرفوض | PASS |
| حذف كائن التخزين | PASS |
| الرابط لا يخدم بعد الحذف مباشرة | **FAIL — بسبب CDN** |

### نتيجة تستحق التسجيل: تأخّر CDN بعد الحذف

الكائنات العامة تُخدَم عبر CDN بترويسة `cache-control: public, max-age=3600`. بعد حذف الكائن من التخزين:

- الرابط نفسه يعيد **200** مع `cf-cache-status: HIT`
- الرابط مع كاسر تخزين يعيد **400**
- الملف غير موجود في التخزين فعلًا

**الحذف يعمل، لكن CDN يستمر في خدمة النسخة المخزّنة حتى ساعة.** هذا يخص `bm-media` وحده اليوم — محتواه تسويقي عام لا يخضع لإشراف، فالتأخير مقبول فيه. أما وسائط المجتمع فخرجت من هذا السلوك كليًا (أدناه).

### وسائط المجتمع — الحل المطبَّق (Medium #9، 2026-09-13)

`community-media` صار **خاصًا**، ولم يعد له رابط عام دائم. كل عرض يمر بمسار خادمي داخل التطبيق:

```
/community/media/<storage_path>
```

يقرأ المسار هوية المشاهد وصلاحية الإشراف، ثم يسأل `resolveMediaAccess` عند **كل طلب**: المالك يرى ملفه دائمًا (يحتاجه فور الرفع قبل ربطه بصف)، والإشراف يرى المخفي ليحكم عليه، وغيرهما يحتاج منشورًا منشورًا بصاحب نشط أو مشروعًا منشورًا أو ملفًا نشطًا. الرفض **404 لا 403**: وجود الملف نفسه ليس معلومة تُمنح لمن لا يراه.

لماذا هذا لا الروابط الموقّعة: الرابط الموقّع يبقى صالحًا حتى انتهاء مدته مهما أخفى الإشراف المحتوى، بينما الفحص عند كل طلب يجعل القطع لحظيًا. ولأن `resolveCommunityMediaUrl` بقيت متزامنة وتعيد رابطًا من نفس الأصل، لم يتغير أي مستدعٍ وبقي `next/image` يعمل بلا `remotePatterns`.

الكاش `private, max-age=60` عمدًا: تخزين مشترك طويل يعيد الثغرة التي أُغلقت.

| الفحص الحي | النتيجة |
|---|---|
| الرابط العام القديم بعد الترحيل | 400 — مات |
| العرض عبر المسار للزائر المصرّح | 200 `image/png` |
| ترويسة الكاش | `private, max-age=60` |
| **إخفاء المنشور إشرافيًا** | **404 فورًا** |
| الاسترجاع | 200 — عاد |
| تعليق العضو | 404 |
| ثلاث محاولات وصول بمسارات غير مصرّح بها | 404 ×3 |
| حذف كائن عضو آخر | مرفوض |
| الحذف ثم الطلب | 404 |

الترحيل `20260912190000` مطبَّق ومسجَّل. `bm-media` لم يُمَس.

**NOT VERIFIED:** رفع صورة حقيقية عبر واجهة الإدارة (الدخول يتطلب كلمة مرور).

---

## 9. البريد الإلكتروني

**APPLICATION VERIFIED** — وليس END-TO-END DELIVERY VERIFIED.

ما أُثبت: مسار التسجيل يقبل الطلب، وتأكيد البريد مفعّل في المشروع (لا جلسة فورية)، وSupabase يقبل إرسال الرسالة الأولى بلا خطأ SMTP.

ما ظهر: **الرسالة الثانية بعد 518 مللي ثانية رُفضت بـ`429 over_email_send_rate_limit`** — بصمة مزود البريد الافتراضي، وهو موثق بأنه للتطوير لا للإنتاج.

ما لم يُثبت: وصول الرسالة إلى صندوق بريد فعلي. لا سبيل للتحقق منه من داخل النظام.

**OWNER ACTION:** ضبط SMTP مخصص، ثم تسجيل حساب ببريد حقيقي وتأكيد وصول الرسالة.

---

## 10. النسخ الاحتياطي والاستعادة

**NOT VERIFIED — OWNER ACTION.**

الموجود: أداة `supabase db dump` متاحة كمسار نسخ يدوي، وملفات الترحيل في Git هي مصدر الحقيقة لإعادة بناء المخطط في أي بيئة جديدة.

غير المتحقق منه: وجود نسخ احتياطي تلقائي وجدولته ومدة الاحتفاظ وPITR — كلها تابعة لخطة Supabase وتُراجع من لوحة المشروع. ولم تُجرَّب استعادة فعلية، ولن تُجرَّب على الإنتاج.

---

## 11. بوابات الجودة — الحالة النهائية

| البوابة | النتيجة |
|---|---|
| TypeScript | **0 خطأ** |
| ESLint | **0** |
| الاختبارات الآلية | **180 ناجحًا / 557 تأكيدًا** عبر 15 ملفًا |
| اختبار RLS الحي | **38 / 38** |
| بناء الإنتاج | **70 / 70 صفحة**، standalone مُغلَّف |
| تشغيل standalone | يعمل ويستجيب |
| فحص الأسرار | نظيف |
| تلوث بيانات الاختبار | **صفر** |

### ملاحظة بيئية

فشل بناء واحد بـ`JavaScript heap out of memory` عند 52/70، ونجح فورًا عند الإعادة. الجهاز 5.9GB إجماليًا و0.7GB حرًا، وسبعة عمال متوازين. ليس انحدارًا برمجيًا — البناء نجح ثلاث مرات أخرى. لكنه يستحق الانتباه: **بيئة بناء منخفضة الذاكرة قد تفشل على Hostinger أيضًا.**

---

## 12. تنظيف بيانات الاختبار

كل بيانات الاختبار أُنشئت بمعرّفات صريحة (`bmnet-rlstest-`، `bmnet-uitest-`، `smtp-probe-`، نطاق `example.invalid`) ثم حُذفت.

التحقق النهائي:

| الجدول | الصفوف |
|---|---|
| `auth.users` | 2 (الموظفان) — **صفر حسابات اختبار** |
| `community_profiles` وكل جداول المجتمع | 0 |
| `media` | 14 (الأصلية) |
| `contact_messages` / `corporate_requests` | 0 |
| كائنات `bm-media` | 14، صفر بقايا اختبار |
| كائنات `community-media` | 0 |

---

## 13. OWNER ACTION — مرتبة

1. **تغيير كلمة مرور حساب الإدارة.** كانت `123456` على نظام إنتاج حي، ووردت في سجل جلسة عمل.
2. **تفعيل Leaked Password Protection** في Supabase Auth.
3. ~~تعطيل Legacy API keys~~ — **تم.** مؤكد: المفاتيح القديمة مرفوضة والتطبيق يعمل بـ`sb_*`.
4. ~~دمج PR #1~~ — **تم.** `main` يحمل إصلاح المجتمع ورفع الصور.
5. ~~قرار صور المجتمع~~ — **تم.** bucket خاص ومسار عرض يفحص عند كل طلب (§8).
6. **ضبط SMTP مخصص.** بدونه لا يكتمل تسجيل أي عضو — وهذا آخر حاجز على إطلاق المجتمع.
7. **النشر إلى Hostinger.**
8. **مراجعة النسخ الاحتياطي** وخطة الاستعادة من لوحة Supabase.
9. إدخال بيانات التواصل والسوشيال والضريبة والعربون والسياسات من الإعدادات بعد النشر.

---

## 14. NOT VERIFIED — بالاسم

- رحلة لوحة التحكم عبر الواجهة: دخول ← تحرير ← حفظ ← انعكاس على الموقع ← استرجاع. **محجوبة: تتطلب كلمة مرور لا تُستخدم.**
- رفع صورة حقيقية عبر محرر في اللوحة.
- وصول رسالة بريد إلى صندوق فعلي.
- استعادة نسخة احتياطية.
- اختبار حمل وقياس Core Web Vitals.
- مراجعة WCAG 2.1 AA شاملة بقارئ شاشة.
- سلوك التطبيق على Linux / Node 22 في Hostinger.
- صفحتا المحفوظات والمحجوبين بحساب عضو فعلي: مُتحقق منهما بنيويًا (المسار، البوابة، إعادة التوجيه، البناء) لا بصريًا.

---

## 15. القيود المعروفة

- **`db push` مسدود** بـ13 مدخلًا في سجل الترحيلات بلا مقابل محلي. كل ترحيل قادم يسلك: `supabase db query --linked -f <ملف>` ثم `migration repair --status applied <نسخة>`. الحل الرسمي (`repair --status reverted`) ضمن ممنوعات المالك.
- **حفظ الصفحة الرئيسية ليس معاملة ذرية:** يعيد كتابة 14 جدولًا. فشل في المنتصف يترك الصفحة نصف محدّثة. العلاج دالة قاعدة واحدة، ولا يمكن اختباره دون الكتابة على محتوى حقيقي.
- **`toArabicDbError` يمرر 160 حرفًا من رسالة القاعدة** إلى العميل في الحالة العامة. نمط قائم في كل التطبيق، ليس من هذا الفرع.
- **حدّ معدل النماذج في ذاكرة العملية:** كافٍ لنشر بعملية واحدة، ليس حدًا موزعًا، ومفتاح المرسِل من ترويسات قابلة للانتحال.
- **حسابا الإدارة كلاهما بدور `owner`.** الأفضل حصره بواحد.
- تحذير إهمال `middleware` في Next 16 · `noImplicitAny: false` · `reactStrictMode: false`.

---

## 16. وثائق مرتبطة

| الملف | المحتوى |
|---|---|
| [TODO.md](TODO.md) | المهام مصنّفة: جاهز · محجوب على المالك · يحتاج بيئة |
| [worklog.md](worklog.md) | سجل التنفيذ مهمة بمهمة مع نتائج الفحص |
| [memory.md](memory.md) | القرارات المرقّمة (D-107 وما بعدها) |
| [prd.md](prd.md) | نطاق المنتج ومعايير القبول |
| [design.md](design.md) | نظام التصميم والهوية |
| [supabase/MIGRATION_RECONCILIATION.md](supabase/MIGRATION_RECONCILIATION.md) | انحراف سجل الترحيلات وما جرى فيه |
| [docs/MOYASAR_READINESS.md](docs/MOYASAR_READINESS.md) | متطلبات تفعيل الدفع |
