# تقرير المشروع — بيت المصور / bmnet

تاريخ التقرير: **12 سبتمبر 2026**. المستودع: [atharz1980-oss/bmnet](https://github.com/atharz1980-oss/bmnet). مرجع كود التطبيق الذي يستند إليه التقرير: **3d5ef3432d6d3ffd72820473690100f5bd44d305** على main. أي commit توثيق لاحق لا يعني إعادة اختبار التطبيق كاملًا.

## 1. النتيجة العامة

المشروع يعمل محليًا ويضم موقعًا عامًا عربيًا، ولوحة إدارة محتوى مرتبطة بـSupabase، ومجتمع المصورين Community V1. أُصلحت أخطاء الاستقرار في مراجعة سابقة ودُفعت الإصلاحات إلى main. أكد المالك تشغيل النسخة على Windows، وأثبت اختبار حي لاحق أن تغيير عنوان الرئيسية واختيار دورة قادمة مع موعدها يُحفظ في القاعدة ويظهر للزائر بعد تحديث الصفحة.

**التقييم: جاهز للتشغيل المحلي، مع قيود موثقة.** لا يصح وصفه بأنه خالٍ من جميع المشكلات، أو نظام دفع وتسجيل مكتمل، أو معتمد أمنيًا بالكامل على Production. لا تختبر بوابات TypeScript والبناء صلاحيات قاعدة الإنتاج أو وصول البريد، كما أن بعض محررات الصور ما زالت تستخدم معاينة محلية فقط.

أعمال هذه الجلسة تحديث وثائق وتقرير وفحوصات محلية؛ لم تتضمن تعديل التطبيق أو SQL أو بيانات المستخدمين. التجربة الحية المذكورة تمت في الجلسة السابقة بإذن المالك واستُعيد بعدها المحتوى الأصلي.

## 2. مصادر الأدلة وحدودها

| المصدر | ما يثبته | ما لا يثبته |
|---|---|---|
| الكود وpackage.json وbun.lock | البنية والوظائف والاعتماديات الحالية | سلامة تشغيل كل مسار في Production |
| SQL migrations والاختبارات | أسماء الجداول والعقد الموجود في Git | تطابق كل سياسات القاعدة البعيدة الآن |
| سجلات مراجعة الاستقرار | نجاح الفحوصات والبناء وفحص الأسرار آنذاك | استمرار خلو الاعتماديات من ثغرات مستقبلية |
| تجربة CMS في 2026-09-12 | حفظ حقيقي من الإدارة وقراءة مجهولة واستعادة المحتوى | صحة جميع CRUD أو المجتمع أو إرسال البريد |
| إفادة المالك | التشغيل المحلي وتطبيق Community DB على Production | تدقيق مستقل كامل للبنية البعيدة |
| الوثائق التاريخية | سجل مراحل وقرارات ونتائج سابقة | تفويض إعادة تنفيذ migrations أو نتائج اختبار حديثة |

لم تُشغّل migrations أو اختبارات RLS الكتابية على Production، ولم يُعد فحص أعداد السياسات أو الفهارس أو سجل migrations البعيد في هذه الجلسة. الأرقام التاريخية في ملفات checkpoint لا تُعرض هنا باعتبارها تحققًا جديدًا.

## 3. المنتج والوظائف

### الموقع العام

يعرض المركز والدورات وتفاصيلها ومناهجها ومواعيدها ومدربيها، ومسارات التعلم والتسعير المشتق، والمقالات، وطلبات تدريب الشركات، والتواصل والسياسات. توجد روابط وmetadata وخريطة موقع و404 حقيقية في المسارات المعالجة. الواجهة عربية RTL وتستخدم IBM Plex Sans Arabic وInter.

المسارات الأساسية: `/`، `/about`، `/courses`، `/courses/[slug]`، `/paths`، `/paths/[slug]`، `/blog`، `/blog/[slug]`، `/contact`، `/corporate-training`، `/policies/[slug]`.

### لوحة الإدارة

تتضمن إدارة الدورات والمواعيد والمناهج والمدربين والمسارات، وأقسام الرئيسية، والتقييمات والمدونة، وطلبات الشركات وملاحظاتها وحالاتها، والوسائط، والصفحات القانونية والإعدادات، والمستخدمين والأدوار، والإشراف على المجتمع. المسارات الإدارية داخل مجموعة `(dashboard)`؛ اسم المجموعة لا يظهر في URL.

الدخول عبر `/admin/login`. صلاحية الموظف تأتي من `profiles` و`roles` و`role_permissions` بعد التحقق من حساب Supabase Auth وحالة الموظف `active`. التعريف البرمجي الحالي يحوي **16 وحدة صلاحيات**، لا 15 كما تقول بعض التعليقات القديمة. الأدوار النظامية خمسة: owner وadmin وcontent-editor وcourse-manager وfinance.

حساب Auth وحده لا يمنح الدخول للإدارة. أعضاء المجتمع في `community_profiles`، والموظفون في `profiles`؛ لا يجوز الخلط بينهما أو تحويل جميع أعضاء المجتمع إلى موظفين.

### Community V1

المتاح في الكود: تسجيل ودخول، ملف شخصي عام ومحرر ملف، خلاصة منشورات صور، إعجاب وتعليقات ومحفوظات، متابعة وحجب، مشاريع أعمال ووسائطها، اكتشاف المصورين، إشعارات داخلية، بلاغات وإشراف إداري. المسارات تشمل `/community` و`/community/photographers` و`/community/u/[username]` و`/community/profile` و`/community/notifications` و`/admin/community`.

اختبارات المجتمع الحالية تغطي المحولات والتحقق وبعض الاستعلامات بعقد HTTP محاكى. رحلة حية شاملة بحسابين تتضمن النشر والتفاعل والحجب والإشراف لم تُنفذ في مراجعة الاستقرار أو تجربة CMS اللاحقة.

### ما ليس منتجًا مكتملًا الآن

- إعدادات Moyasar وTabby وTamara موجودة، لكن لا Checkout أو دفع أو Webhooks مالية فعلية.
- لا نظام LMS مكتمل لتسجيل الطلاب وتقديم الدروس وتتبع تقدمهم.
- لا Google Reviews API أو WhatsApp API لإرسال رسائل خلفية؛ لا يُساوى رابط التواصل بتكامل إرسال.
- ميزات Community V2، ومنها الدردشة والمجموعات والوظائف وStories وLive وPush والاشتراكات وترتيب AI، خارج النطاق.
- عدد المتدربين و«الإيرادات» في لوحة الإدارة مشتقان من أعداد المسجلين وأسعار الجلسات. **الإيرادات المعروضة ليست إثباتًا لتحصيل مالي**؛ لا يوجد دفتر دفعات يعتمد عليه هذا الحساب.

## 4. المعمارية وتدفق البيانات

| الطبقة | التنفيذ |
|---|---|
| التطبيق | Next.js App Router، React، TypeScript |
| الواجهة | Tailwind CSS، shadcn/Radix، مكونات عربية RTL |
| القراءة العامة | `loadPublicView` ثم `derivePublicCms` ثم `PublicCmsProvider` |
| الإدارة | جلسة وصلاحيات ثم تحميل CMS وحصر البيانات المسلسلة بحسب الدور |
| كتابة الإدارة | Server Actions مع تحقق صلاحيات ثم عميل خدمة Supabase |
| كتابة المجتمع | Server Actions بعميل الجلسة الخادمي لتطبيق RLS |
| التخزين | `bm-media` لوسائط CMS و`community-media` للمجتمع |
| الإنتاج المحلي | بناء standalone عبر `scripts/build.mjs` وتشغيل `scripts/start.mjs` |

Supabase هو مصدر بيانات CMS الفعلي. لم تعد `localStorage` وسيلة حفظ الإدارة أو مزامنة الموقع العام. توجد حالة React للمسودات وبيانات ثابتة احتياطية لبعض حالات فشل التحميل، ولذلك يمكن أن تظهر صفحة تبدو سليمة حتى مع مشكلة اتصال؛ يجب التحقق من الحفظ وقراءة القاعدة عند التشخيص.

الموقع العام يستخدم عميلًا مستقلًا بالمفتاح العام، فلا تتحول زيارة الموظف للصفحات العامة إلى قراءة بصلاحيات خدمة. بيانات الإدارة تُحمّل في الخادم وتُصفّى قبل إرسالها للمتصفح. وجود المفتاح السري على الخادم يستلزم بقاء التحقق من الصلاحيات داخل الإجراءات؛ RLS لا يقيد عميل الخدمة بالطريقة نفسها التي يقيد بها العضو.

## 5. البيئة والاعتماديات

| البند | الحالة المثبتة |
|---|---|
| Next.js | النسخة المختبرة 16.3.4؛ package.json يستخدم نطاق تحديث وbun.lock يضبط التثبيت |
| React / TypeScript / Tailwind | عائلات 19 / 5.9 / 4 |
| Supabase JS / SSR | مثبتان على 2.115.0 / 0.12.6 |
| بيئة الفحص | Node 24.18.0 وBun 1.4.0 |
| الحد الأدنى المعلن | Node >=22 وBun >=1.3 |
| مخرجات البناء | standalone؛ نسخ public وstatic آليًا |
| TypeScript | strict=true مع noImplicitAny=false وskipLibCheck=true؛ أخطاء البناء غير متجاهلة |
| React Strict Mode | false حاليًا |
| Middleware | `middleware.ts` قائم؛ تحذير اصطلاح الإهمال موجود في البناء |

أُزيل قالب Prisma/SQLite غير المستخدم وبعض الاعتماديات غير المستخدمة. لا يحتاج التشغيل `prisma generate` أو Bash أو أوامر `cp -r` اليدوية. البناء يستخدم خطوط Google عبر next/font ويحتاج اتصالًا لتنزيلها عند الحاجة.

متغيرات البيئة المطلوبة فقط: `NEXT_PUBLIC_SUPABASE_URL`، `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`، `SUPABASE_SECRET_KEY`. تحفظ القيم محليًا في `.env.local` بجوار package.json، وتبقى `.env.example` بثلاث قيم فارغة. المفتاح السري للخادم فقط. إعدادات `NEXT_PUBLIC_` تدخل حزمة المتصفح عند البناء، لذلك تغييرها يحتاج إعادة بناء؛ تغيير محتوى CMS لا يحتاج ذلك.

## 6. عقد قاعدة البيانات

توجد **9 ملفات migrations** في المستودع. الجداول الفعلية للمجتمع في SQL هي:

| المجال | الجداول |
|---|---|
| الهوية | community_profiles |
| النشر | community_posts، community_post_media |
| التفاعلات | community_post_likes، community_post_comments، community_saved_posts |
| العلاقات | community_follows، community_user_blocks |
| معرض الأعمال | community_portfolio_projects، community_portfolio_media |
| التنبيهات والإشراف | community_notifications، community_content_reports |

كلها ببادئة `community_`. الأسماء المختصرة مثل posts أو notifications في الخطة القديمة ليست أسماء الجداول المعتمدة للمجتمع. تم تصحيح عرضها في التوثيق الحالي.

لا يوجد نوع Supabase `Database` مولد شامل؛ يستخدم التطبيق row interfaces يدوية. اختبار العقد يفحص أسماء الاستعلامات الحرفية الموجودة في التطبيق مقابل migrations وتفعيل RLS لجداول المجتمع الاثني عشر. هذا فحص مهم لكنه لا يثبت كل عمود أو شرط سياسة أو استعلام مركب في قاعدة Production.

يوجد توثيق تاريخي لاختلاف أرقام سجل تطبيق Community عن ملفات Git. **لا يُسمح بمعالجته تلقائيًا**. قاعدة المجتمع مطبقة بالفعل، وتعليمات المالك تمنع repair أو إعادة migrations دون طلب منفصل. مجرد تنزيل الكود وتشغيله لا يتطلب تشغيل SQL.

## 7. نتيجة تجربة الحفظ الحية

أُجريت التجربة على `http://127.0.0.1:3000` المتصل بمشروع Supabase الذي أعده المالك، مع سياق متصفح إداري وسياق زائر مجهول مستقل. جلسة الاختبار مؤقتة؛ لم تتغير كلمة مرور أو صلاحية حساب ولم يُرسل بريد.

| الخطوة | النتيجة |
|---|---|
| قراءة الحالة الأصلية | وضع يدوي لدورة الإضاءة الاستوديوهية، والموعد فارغ |
| تفسير العرض الأصلي | الكود يرجع للاختيار التلقائي ويعرض ورشة أساسيات التصوير |
| تعديل مؤقت | عنوان Hero واختيار موعد الإضاءة بتاريخ 2026-09-21 |
| الحفظ من لوحة التحكم | PASS؛ نحو 9.362 ثانية في هذه العينة |
| التحقق من القاعدة | العنوان ومعرّف الموعد محفوظان |
| تبويب الزائر المفتوح | بقي كما هو قبل إعادة التحميل؛ لا تحديث لحظي |
| إعادة تحميل الصفحة العامة | PASS؛ ظهرت الدورة والموعد والعنوان، نحو 0.824 ثانية للعينة |
| إعادة فتح الإدارة | البيانات المعدلة باقية |
| الاستعادة | العنوان والاختيارات الأصلية أُعيدت، وعاد العرض الأصلي |
| فحص المحتوى المحيط | تطابق محتوى 14 جدولًا للرئيسية بعد تجاهل timestamps ومعرّفات الصفوف المعاد إنشاؤها |
| أخطاء JavaScript الملتقطة | صفر |

هذه الأزمنة قياسات عينة وليست حدًا مضمونًا للأداء. الحفظ يستدعي `revalidatePath('/', 'layout')`، وroot layout يحدد `revalidate=300` كآلية ISR عند الطلب. لا يوجد Realtime لتحديث التبويب المفتوح، ولا يلزم restart/build لتعديل المحتوى. لا تُعمّم النتيجة على نسخة مستضافة أخرى لها كاش مستقل؛ لم تُختبر تلك النسخة في هذه التجربة.

نتائج الخطوات محفوظة في [سجل اختبار CMS المنقّح](docs/evidence/cms-homepage-test-2026-09-12.json)، دون أي بيانات جلسة أو مفاتيح.

للاختيار اليدوي: اختر الدورة والموعد، ثم احفظ وانتظر النجاح ثم F5. إذا لم تحدد موعدًا فقد ترى دورة أخرى نتيجة الرجوع التلقائي، وليس بالضرورة فشل الحفظ.

## 8. إصلاحات الاستقرار المنجزة

أُنجزت في commit واحد `fix: stabilize bmnet application`، شمل 45 ملفًا مقارنة بأساس المراجعة `4980e8a`:

- إصلاح أوامر Windows والبناء وتغليف standalone وتحميل البيئة، وإزالة تجاهل أخطاء TypeScript.
- تحديث اعتماديات لمعالجة تنبيهات أمنية، وإزالة قالب Prisma واعتماديات وأوامر اختبار قالب غير مستخدمة.
- تصحيح ربط صاحب الإشعار بعلاقة FK المحددة في Community، وترقيم صفحات الخلاصة وحالة الإعجاب والحفظ في الملفات العامة.
- التحقق من التواريخ ومسارات الوسائط وملكية التعديلات ومعالجة التحديث الذي لا يؤثر في صف، وإبقاء فشل API الخلاصة فشلًا واضحًا.
- حصر بيانات الإدارة حسب الصلاحيات، واشتراط نشاط الموظف، ومنع تجاوز الصلاحيات عند التفويض، وتحسين حماية المالك وفحص صلاحية النشر.
- تصحيح تدفق دعوة الموظفين وتعيين كلمة المرور، وصلاحيات المدفوعات وترتيب حذف صلاحيات الدور.
- منع حذف وسائط CMS المستخدمة، وإصلاح اشتراكات React وحالات التحميل.

لم تتغير migrations في هذه الإصلاحات. تفاصيل النطاق والقيود في [STABILITY_AUDIT.md](STABILITY_AUDIT.md).

## 9. بوابات الجودة

| الفحص | آخر نتيجة موثقة | نطاق هذه الجلسة |
|---|---|---|
| تثبيت frozen lockfile | PASS في مراجعة الاستقرار | لم تُغيّر dependencies ولم يُكرر التثبيت |
| TypeScript | 0 أخطاء | أعيد تشغيله ونجح |
| ESLint | 0 أخطاء و0 تحذيرات | أعيد تشغيله ونجح |
| الاختبارات | 70 PASS، 0 FAIL، 214 assertions، 5 ملفات | أعيد تشغيلها ونجحت |
| Production build | PASS؛ Next 16.3.4، توليد 67/67 | آخر بناء محفوظ؛ لم يُكرر لتعديل وثائق فقط |
| Browser smoke | 16 مسارًا + 4 فحوص موبايل عند 360px | من مراجعة الاستقرار، دون حسابات حينها |
| Browser CMS live | PASS للسيناريو المحدد أعلاه | دليل لاحق بعد تهيئة البيئة |
| Dependency audit | bun audit أعاد كائنًا فارغًا | نتيجة مراجعة الاستقرار، وليست ضمانًا مستقبليًا |
| Secrets scan | Gitleaks: لا أسرار بالمصدر أو تاريخ Git وقت المراجعة | فحص مجموعة وثائق التسليم الحالية PASS: لا أسرار؛ لا إدراج env أو جلسات |

أنواع الاختبارات: تحويل حالات الدورات والبيانات والروابط، قواعد المجتمع والتخزين، استعلامات Supabase حقيقية مع HTTP محاكى، مقارنة أسماء الجداول بالـSQL، وحواجز بيانات وصلاحيات الإدارة. لا تُساوى هذه الاختبارات باختبار E2E شامل أو ضغط أو تغطية أمان لكل مسار.

## 10. القيود والمخاطر المتبقية

| الأولوية | المسألة | الأثر والعمل المطلوب |
|---|---|---|
| عالية | سياسات Community notification UPDATE | مالك الإشعار يستطيع عبر REST تعديل أعمدة تتجاوز read_at وفق SQL القائم؛ تحتاج صلاحيات أعمدة/سياسات واختبارات قاعدة منفصلة |
| عالية | بعض شروط تعديل child media وحالة العضو | فحوص الملكية أو نشاط العضو غير مكتملة في كل سياسة؛ فحص التطبيق وحده لا يغلق REST المباشر |
| عالية تشغيليًا | الحفظ عبر طلبات متعددة | قد تنجح بعض الكتابات وتفشل التالية، بما فيها إعادة إنشاء قوائم الرئيسية؛ يلزم تصميم حفظ ذري أو تعويض واختبار الفشل ضمن مهمة منفصلة |
| متوسطة | ImageUpload في عدة محررات | معاينة Object URL فقط، دون رفع دائم؛ يلزم ربط واضح بمكتبة الوسائط ومنع اعتبار blob مسارًا دائمًا |
| متوسطة | اختلاف اختيار الموعد الممتلئ | الجسر العام يمكن أن يقبل full ضمن المواعيد العامة بينما معاينة الإدارة تحذر/ترفضه؛ يحتاج توحيد قاعدة المنتج واختبارها |
| متوسطة | التلقائي لا يستبعد التواريخ الماضية بساعة النظام | موعد قديم ما زال open/upcoming قد يستمر مرشحًا؛ يلزم حسم سياسة التشغيل قبل تغيير السلوك |
| متوسطة | بيانات وSEO النشر | siteConfig يحتوي قيمًا افتراضية ودومينًا يحتاج تأكيدًا؛ يجب اختبار ما ينعكس فعلًا من إعدادات SEO على HTML |
| متوسطة | البريد والدعوات | الكود موجود، لكن SMTP وRedirect URLs والتسليم وقبول الدعوة لم تُختبر حيًا في هذه المراجعة |
| متوسطة | تحميل بريد المستخدمين | loadUserEmails يجلب صفحة واحدة بـperPage=500 دون pagination؛ قابل لفقد عرض بريد بعض الموظفين عند زيادة حسابات Auth |
| متوسطة | اختبارات المجتمع الحية | لم يكتمل سيناريو بحسابين مع الحجب والإشراف وRLS وملكية الصور |
| منخفضة/متابعة | generated types وCI | لا Database type مولد شامل ولا workflows في .github؛ فحوص حالية محلية |
| تشغيلية | Storage عام | إخفاء منشور لا يلغي الوصول لرابط صورته العام؛ ليس ضمان خصوصية للملف |
| تشغيلية | عرض الحجب | قائمة العضو تعرض حظره الصادر؛ الحجب الوارد ليس متاحًا بهذه القراءة، مع تحقق SQL للتفاعل المتبادل |

الملاحظات الجديدة أثناء إعداد التقرير مبنية على قراءة الكود، ولم تُستغل أو تُختبر بكتابات تخريبية. إعداد التقرير لا يعني تنفيذ هذه الإصلاحات أو تفويض تغيير قاعدة Production.

## 11. ما يحتاج اختبارًا قبل اعتماد إنتاج شامل

الأولوية: مراجعة سياسات Community في بيئة اختبار منفصلة، ثم رحلة عضوين كاملة تشمل الصور والحجب والإشراف والإشعارات، واختبار استعادة الحفظ بعد فشل متوسط. يلي ذلك إكمال حقول الصور واختبار SMTP والدعوات والصلاحيات الدنيا، ومطابقة الدومين وSEO والروابط على النسخة المستضافة، ثم قياس حمل ونسخ احتياطي واستعادة وتدقيق وصول شامل.

كل بند عمل مستقل قابل للمراجعة. لا تُنفذ migrations أو إصلاح سجلها أو كتابة بيانات اختبار على Production تلقائيًا استنادًا لهذا الجدول.

## 12. تشغيل Windows

من CMD بعد تثبيت Git وNode وBun:

```cmd
git clone https://github.com/atharz1980-oss/bmnet.git
cd bmnet
bun install --frozen-lockfile
if not exist .env.local copy .env.example .env.local
notepad .env.local
```

احفظ قيم البيئة الصحيحة، ثم:

```cmd
bun run typecheck
bun run lint
bun run test
bun run build
bun run start
```

افتح `http://127.0.0.1:3000`. لتشغيل نسخة مبنية سابقًا يكفي `bun run start`. أوقف خادم المشروع بـCtrl+C قبل إعادة البناء. للتطوير استخدم `bun run dev`. احتفظ بنفس اسم المضيف أثناء تسجيل الدخول لأن localhost و127.0.0.1 لا يشتركان في cookies.

للتحديث من GitHub بعد إيقاف الخادم ومع خلو الملفات المتتبعة من تعديلات محلية متعارضة:

```cmd
git pull --ff-only origin main
bun install --frozen-lockfile
bun run build
bun run start
```

لا تشغّل SQL للتثبيت المحلي ولا ترفع `.env.local`. إذا كانت البيئة تشير إلى Supabase Production، فحفظ المحتوى من الموقع المحلي يكتب إلى القاعدة نفسها، وليس نسخة محلية معزولة منها.

## 13. الوثائق المحدثة

- [prd.md](prd.md): متطلبات المنتج الحالي وحدوده ومعايير القبول؛ لم يعد يصف الإدارة كـMock بلا Backend.
- [memory.md](memory.md): حالة مختصرة قابلة للاستئناف، خريطة الملفات، القيود، D-101..D-106، وسجل الجلسات الأخيرة.
- [README.md](README.md): مدخل التشغيل والفهرس والصلاحيات وسلوك الحفظ وحدود الصور.
- [design.md](design.md): تصحيح توصيف مرحلة الإدارة والوسائط مع إبقاء الهوية والتصميم.
- [STABILITY_AUDIT.md](STABILITY_AUDIT.md): فصل مراجعة الاستقرار الأولى عن اختبار CMS الحي اللاحق.
- [worklog.md](worklog.md): إضافة أعمال الاستقرار وتجربة CMS وجلسة التوثيق دون حذف السجل القديم.
- وثائق Community وmigrations/checkpoints: تصحيح الحالة وأسماء الجداول والتنبيه إلى تاريخية تعليمات التنفيذ السابقة.
- [docs/history/prd-phase2-2026-09-02.md](docs/history/prd-phase2-2026-09-02.md) و[docs/history/memory-before-2026-09-12.md](docs/history/memory-before-2026-09-12.md): النسخ السابقة محفوظة للمراجعة التاريخية.

تحقق التسليم: سلامة محتوى الأرشيفين مقارنة بنسخهما في Git، وروابط الوثائق المحلية، واتزان الأسوار البرمجية، وسلامة UTF-8، وبقاء .env.example بلا قيم، وعدم تغيير الكود أو migrations. مجموع ملفات التوثيق الجديدة والمعدلة 14، بما فيها سجل اختبار CMS المنقح.

لا يحتوي التقرير بيانات اعتماد أو كلمات مرور أو cookies أو قائمة مستخدمين. هذه الوثائق مرجع للحالة الحالية، والاختبارات غير المنفذة تبقى كذلك حتى يوجد دليل مستقل على نجاحها.
