# قائمة المهام — بيت المصور

آخر تحديث: 2026-09-13 (بعد مرحلة «عام أولًا»). المرجع التفصيلي في [PROJECT_REPORT.md](PROJECT_REPORT.md) و[memory.md](memory.md).

الرموز: **[ج]** جاهز للتنفيذ · **[م]** محجوب على قرار المالك · **[ح]** يحتاج وصولًا أو بيئة غير متاحة.

---

## 0. عاجل — ما بقي على الطاولة

- [ ] **[م]** **SMTP مخصص** — المزود الافتراضي يرفض بعد رسالة واحدة (`429 over_email_send_rate_limit`). **هذا آخر حاجز على إطلاق المجتمع**: بدونه لا يصل بريد تأكيد التسجيل ولا دعوة الإدارة، فلا يكتمل انضمام أي عضو. الإعداد في Supabase → Authentication → Emails → SMTP Settings، ثم ضبط Site URL على `https://baytalmosawer.net` وإضافة `/admin/login` إلى Redirect URLs.
- [ ] **[م]** **غيّر كلمة مرور حساب الإدارة** — `123456` على نظام إنتاج حي، ومكتوبة في سجل جلسة.
- [ ] **[م]** **فعّل Leaked Password Protection** في Supabase Auth — كان سيمنع كلمة كهذه ابتداءً.
- [ ] **[م]** **النشر إلى Hostinger** — `main` جاهز ومبني.

### أُغلق

- [x] ~~ترحيل منح `social_links`~~ — مطبَّق ومسجَّل؛ الروابط تُقرأ من الموقع العام.
- [x] ~~تعطيل Legacy API keys~~ — مؤكد: القديمة مرفوضة والتطبيق يعمل بـ`sb_*`.
- [x] ~~إيداع العمل ودمج PR #1~~ — مدموج إلى `main`.
- [x] ~~`npm run build`~~ — 70/70 صفحة وstandalone.
- [x] ~~مراجعة Auth UX~~ — الموقع «عام أولًا»: نطاق الـmiddleware انحصر في `/admin` و`/community`، ووجهة العودة تعبر الدخول والتسجيل، ودخول الإدارة يرفض حساب المجتمع برسالة. 44/44 حيًا.

## 1. قاعدة البيانات وربط Supabase

### ترحيلات مكتوبة ومنتظرة التطبيق

لم يُطبَّق أي منها. **محجوبة بأمرين**: كلمة مرور القاعدة لـ`supabase link` (يضبطها المالك بنفسه في متغير بيئة)، وإذن صريح بـ`migration repair --status applied` للنسخ الثلاث للمجتمع — سجل الإنتاج منحرف فـ`db push` سيحاول إعادة تنفيذها ويفشل. التفاصيل في [MIGRATION_RECONCILIATION.md](supabase/MIGRATION_RECONCILIATION.md).

- [x] ~~الترحيلات الثلاثة~~ — طُبّقت وسُجّلت. `migration list`: 12 متزامنًا، صفر معلّق.

### عيوب مصححة تنتظر اختبارًا حيًا

التصحيحات مُثبتة نصيًا في `tests/community-rls-contract.test.ts` فقط. المطلوب اختبار بحسابين على قاعدة اختبار:

- [x] ~~تأكيد التصحيحات الأربعة سلوكيًا~~ — اختبار حي بحسابين على الإنتاج: **24/24** شاملة التخزين، والقاعدة عادت فارغة. السيناريو في `supabase/tests/community_rls_live.mjs`.

### ثغرات قائمة لم تُعالج

- [x] ~~حذف المنشور أو المشروع لا يحذف ملفاته من التخزين~~ — أُصلح في المسارات الثلاثة (عضو، مشروع، إشراف).
- [x] ~~إخفاء المنشور إشرافيًا لا يخفي صورته~~ — **أُصلح.** `community-media` صار خاصًا، وكل عرض يمر بمسار `/community/media` يفحص الظهور عند كل طلب. مؤكد حيًا: الإخفاء يعيد 404 **فورًا**، والاسترجاع يعيد الظهور. التفاصيل في `PROJECT_REPORT.md` §8.
- [ ] **[ج]** حفظ الصفحة الرئيسية يعيد كتابة 14 جدولًا وليس معاملة ذرية؛ يحتاج RPC واحدة.
- [x] ~~حفظ الدورة يفقد المنهج والمواعيد عند أي فشل~~ — أُغلق بـ`save_course_atomic`: معاملة واحدة، ومطابقة الأطفال بالمعرّف. إثبات تراجع 24/24.
- [ ] **[ج]** `community_content_reports` يمنح `UPDATE` لدور `authenticated` على مستوى الجدول رغم أن السياسة الوحيدة إدارية. تضييق احترازي منخفض الأولوية.
- [x] ~~توليد أنواع `Database`~~ — وُلّدت في `src/types/database.ts` (2497 سطرًا، 53 جدولًا).
- [x] ~~ربط `database.ts` وإصلاح أخطاء الأنواع~~ — 62 خطأً عولجت كلها، ومنها `day_id` و`trainer_id` وحالة المستخدم.
- [ ] **[ج]** `db push` مسدود بـ13 مدخل remote-only. مسار أي ترحيل قادم: `db query --linked -f` ثم `migration repair --status applied`. فتحه نهائيًا يحتاج قرارك برفع الحظر عن `--status reverted`.

## 2. المجتمع — مجتمع المصورين

- [x] ~~الحجب باب لا رجعة منه~~ — صفحة `/community/blocked` لفك الحجب.
- [x] ~~الحفظ بلا مكان يُعرض فيه~~ — صفحة `/community/saved`.
- [x] ~~صفحات الأعضاء بلا روابط بينها~~ — `MemberNav` في layout القسم.

المراحل الـ15 لـV1 منفّذة. ميزات V2 (دردشة، Realtime، Jobs، Groups، Stories، Live، Push، ترتيب AI) **مستبعدة بأمر المالك**؛ لا تُبدأ دون رفع الاستبعاد.

- [x] ~~طبقة RLS بحسابين~~ — 17/17 عبر جلستي عضوين حقيقيتين.
- [x] ~~طبقة التخزين~~ — سياسات `storage.objects` مؤكدة حيًا ضمن الـ24 تأكيدًا.
- [x] ~~الصفحات العامة بالمتصفح~~ — `/community` تعرض حالة الفراغ الصحيحة بدل الخطأ، وصفر أخطاء وحدة تحكم على أربعة مسارات.
- [ ] **[م]** رحلة **الإدارة** بالواجهة: تسجيل دخول ← محرر ← رفع صورة ← إشراف. محجوبة: الدخول يتطلب كلمة مرور ولا أستخدمها.
- [ ] **[ح]** التحقق من وصول رسائل تأكيد التسجيل ودعوات الإدارة فعليًا — يحتاج SMTP معدًّا (البند الأول في §0).
- [ ] **[م]** رفع الاستبعاد عن أي ميزة V2 — قرار المالك.

## 3. الوسائط

- [x] ~~`ImageUpload` معاينة `blob:` فقط~~ — يرفع الآن فعلًا إلى `bm-media` في 11 استخدامًا عبر 9 محررات، بحالة رفع وخطأ صريحتين ومجلد لكل محرر.
- [ ] **[ج]** تأكيد الرفع حيًا عبر المتصفح — التحقق الحالي ساكن على مستوى العقد.

## 3.1 ملاحظة لِما بعد وصول التسجيل في الدورات

- [ ] **[م]** حين يصل التسجيل في دورة: اربط زر الاشتراك بـ`communityLoginHref('/courses/<slug>')` للزائر. الآلية جاهزة ومختبرة؛ ينقصها الفعل نفسه — الحجز اليوم عبر واتساب ولا يحتاج حسابًا.

## 4. التسجيل والدفع

مؤجل بطلب المالك. المرحلة 4.1 شرط للمرحلة 4.2 ولا تُقفز.

### 4.1 نظام الطلبات (غير موجود إطلاقًا)

- [ ] **[م]** جداول الطلبات والتسجيلات.
- [ ] **[م]** حساب السعر على الخادم من موعد الدورة — لا يُعتمد سعر قادم من المتصفح.
- [ ] **[م]** حجز المقعد وتأكيده في معاملة واحدة تقاوم التكرار والتزامن.
- [ ] **[م]** التعامل مع المقاعد الممتلئة والدفع المتأخر دون ادعاء حجز مؤكد.

### 4.2 الدفع الفعلي

- [ ] **[م]** نموذج بطاقة ميسر والتحقق من الدفع بمطابقة المعرّف والمبلغ والعملة والطلب والبيئة.
- [ ] **[م]** Webhook آمن للتكرار.
- [ ] **[م]** اختبار Sandbox كامل ثم قائمة الإطلاق.
- [ ] **[ج]** ضبط `PAYMENTS_ENCRYPTION_KEY` في Hostinger — **وحده** ما يُعطّل أزرار
  «إدخال المفاتيح» و«اختبار الاتصال» و«حذف المفاتيح» في `/admin/settings/payments`.
  قيمة base64 لـ32 بايت عشوائية، ثم إعادة تشغيل الموقع. باقي الصفحة يعمل:
  «حفظ إعدادات السداد» حُفظ فعلًا على الإنتاج في 2026-09-15.

## 5. بيانات ينتظرها الموقع من المالك

مؤجلة بطلب المالك، لكن الموقع منشور بها الآن.

- [ ] **[م]** الهاتف والبريد والعنوان الفعلية — الموقع يعرض `+966551234567` التجريبي.
- [ ] **[م]** روابط منصات التواصل.
- [ ] **[م]** الحالة الضريبية والرقم الضريبي والنسبة.
- [ ] **[م]** حسم سياسة الاسترجاع: 48 أم 72 ساعة.
- [ ] **[م]** السداد: كامل أم عربون، وقيمته ومهلة الباقي.
- [ ] **[م]** تحديث نص الخصوصية ليشمل بيانات التسجيل ومعالج الدفع.

## 6. جودة وتشغيل

- [ ] **[ج]** تحذير إهمال `middleware.ts` في Next 16.
- [ ] **[ج]** `noImplicitAny: false` و`reactStrictMode: false` قائمان منذ ما قبل الاستقرار.
- [ ] **[ج]** مراجعة WCAG شاملة.
- [ ] **[ح]** اختبار حمل.
- [ ] **[ح]** نسخ احتياطي واستعادة موثقان ومجرَّبان.

---

## قواعد لا تُخالَف

- قاعدة Community مطبقة على Production. لا `db reset` ولا `drop` ولا `truncate` ولا `force migration` ولا `migration repair` دون طلب جديد صريح.
- التعديلات من التطبيق المحلي تكتب إلى قاعدة Production نفسها إن أشارت البيئة إليها.
- لا تُنشر بيانات قانونية أو هاتف أو بريد مفترضة، ولا تُختار نسبة ضريبة أو قيمة عربون دون اعتماد المالك.
- نجاح البناء المحلي لا يثبت نجاح النشر ولا اعتماد أمان Production.
